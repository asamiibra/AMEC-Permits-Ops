import fs from 'node:fs'; const t=fs.readFileSync('frontend/src/AMECWork.tsx','utf8'); const rows=[['Retry','READ_REFRESH',t.includes('onClick={load}')],['KPI filter','READ_REFRESH_TRIGGER',t.includes('onClick={() => update({ kpi:')],['Clear filters','READ_REFRESH_TRIGGER',t.includes('const clearFilters = () => update')],['Team filter','READ_REFRESH_TRIGGER',t.includes('update({ team:')],['Work filter','READ_REFRESH_TRIGGER',t.includes('update({ domain:')]]; console.log(JSON.stringify(rows)); if(rows.some(x=>!x[2])) process.exit(1);
