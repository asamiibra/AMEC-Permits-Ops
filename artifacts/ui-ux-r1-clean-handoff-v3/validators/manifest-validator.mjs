import fs from 'node:fs'; import path from 'node:path'; import crypto from 'node:crypto'; const root='artifacts/ui-ux-r1-clean-handoff-v3'; const mf=path.join(root,'SHA256SUMS'); const listed=fs.readFileSync(mf,'utf8').trim().split('\n').filter(Boolean).map(x=>x.split('  ')[1]); const files=[]; function walk(d){for(const n of fs.readdirSync(d)){const p=path.join(d,n);if(fs.statSync(p).isDirectory())walk(p);else if(p!=='artifacts/ui-ux-r1-clean-handoff-v3/SHA256SUMS'&&!p.endsWith('/24-azure-handoff.json'))files.push(p)}} walk(root); const rel=p=>p.replace(/^artifacts\//,'artifacts/'); const set=new Set(listed); const missing=files.filter(p=>!set.has(rel(p))); const unlisted=listed.filter(p=>!files.some(f=>rel(f)===p)); console.log(JSON.stringify({listed:listed.length,files:files.length,missing,unlisted})); if(missing.length||unlisted.length||new Set(listed).size!==listed.length)process.exit(1);
