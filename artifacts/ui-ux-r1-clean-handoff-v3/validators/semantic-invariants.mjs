import fs from 'node:fs'; const a=JSON.parse(fs.readFileSync('artifacts/ui-ux-r1-clean-handoff-v3/05-api-family-ledger.json')).families.find(x=>x.API_FAMILY_ID==='API-001'); const v=JSON.parse(fs.readFileSync('artifacts/ui-ux-r1-clean-handoff-v3/06-value-binding-ledger.json')).values.filter(x=>x.API_FAMILY==='API-001'); const ac=JSON.parse(fs.readFileSync('artifacts/ui-ux-r1-clean-handoff-v3/07-action-ledger.json')).actions.filter(x=>x.SURFACE==='AMECWork'&&x.API_BACKED_BEHAVIOR); const ok=a.BACKEND_MATCH_STATUS==='EXACT_BACKEND_ROUTE'&&a.DISPOSITION==='CANONICAL_BACKEND_PROJECTION'&&v.every(x=>x.BACKEND_ROUTE_STATUS==='EXACT_BACKEND_ROUTE')&&ac.every(x=>x.API_FAMILY_ID==='API-001'&&x.ACTION_CLASS!=='LOCAL_PRESENTATION_ONLY'); console.log(JSON.stringify({ok,api:a,values:v.length,actions:ac.length})); if(!ok) process.exit(1);
