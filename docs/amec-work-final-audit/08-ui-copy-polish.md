# UI copy polish

The homepage keeps only the `SYNTHETIC PROTOTYPE` badge and `Test data and simulated integrations` boundary copy. The large Permit-specific warning and duplicate right-side synthetic block are hidden on AMEC Work. The descriptor is `PROPOSAL • CONTRACT • PERMIT`; actions use Open Proposal, Open Contract, Open Permit, Review source, or Review draft.
