# Canonical scope and lineage

Handover is scoped to `ServiceEngagement` under an exact `ContractRevision`. A Contract may have multiple services; closing one service does not close its siblings, the Contract, or the Project.
