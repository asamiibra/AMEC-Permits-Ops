# Proposal list

`GET /api/bd/proposals` and `/bd/proposals` provide search, stage/location filtering, Proposal, project reference, client, stage, amount, last activity, and workspace navigation. Client labels fall back to the canonical ClientAccount display name.
