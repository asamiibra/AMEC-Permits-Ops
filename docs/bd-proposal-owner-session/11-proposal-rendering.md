# Proposal rendering

Accepted Proposal output is deterministic, downloadable, and returned with `X-Artifact-Hash`. The artifact records the accepted revision and canonical Dashboard template provenance.
