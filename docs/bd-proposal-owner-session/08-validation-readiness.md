# Validation and readiness

Validation is deterministic and returns blockers, warnings, and readiness. It checks required fields, client/scope consistency, price/duration, all four source lanes, canonical template/checklist resolution, and conflict flags before Accept.
