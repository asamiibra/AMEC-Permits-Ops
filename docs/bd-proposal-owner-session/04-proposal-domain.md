# Proposal domain

The session uses the existing Opportunity record as the transactional Proposal record and adds ProposalSourceEvidence, ProposalAcceptedRevision, ProposalOutputArtifact, and ProposalOwnerSetting. No Dashboard table stores Proposal-specific source bytes.
