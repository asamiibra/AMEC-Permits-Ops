# Browser E2E

The deployed `/bd/proposals` list and workspace were visually inspected in the in-app browser. Verified visible sections include Proposal list, four source lanes, structured fields, Human AMEC review input, Validation & Gaps, and Human control boundary.
