# AMEC input and provenance

Human AMEC Input is stored separately inside the transactional Proposal fields and returned with provenance. The implementation has no autonomous AI mutation path; the AI seam is explicit and disabled.
