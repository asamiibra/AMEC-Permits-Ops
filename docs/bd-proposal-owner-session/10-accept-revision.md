# Accept and revision

Accept is a human-controlled BD capability. It creates an immutable normalized snapshot and hash, records the accepted revision, and generates the Proposal and Checklist output artifact rows. Engineering receives read-only access and cannot Accept.
