# Synthetic Adjudication Disposition

> **DEMONSTRATION BASELINE — SYNTHETIC DATA — NOT CLIENT APPROVED**

Every generated Week 1–3 `Conflict` now has an explicit disposition. The canonical golden-path project is `GHCE-2026-0142`; its historical `PRJ-2026-001` label is retained only as a `LegacyFixtureAlias`. Conflicts on the canonical project are `RESOLVED` using canonical title-deed evidence and the Synthetic Requirement Steward. Conflicts on non-golden-path variants are `ACCEPTED` as intentionally ambiguous synthetic variants, with the runtime expectation that later human review is required.

No conflict was silently removed from the seed. The disposition is recorded with resolver, resolution, timestamp, audit event, and source fixture metadata.
