# Build Authorization Boundary

> Evidence class: `SYNTHETIC_IMPLEMENTATION_EVIDENCE`

PermitOps is currently on the `SYNTHETIC DEVELOPMENT / PROTOTYPE TRACK`.

| Track | Current status | Evidence basis |
|---|---|---|
| TRACK_A — synthetic development prototype | `ACTIVE_SYNTHETIC` | Repository implementation, canonical synthetic fixture, simulator tests |
| TRACK_B — formal client build | `NOT_AUTHORIZED` | Stage 2 baseline is DRAFT; Sign-off C is a draft only; no signed client authorization found |
| TRACK_C — live production | `NOT_AUTHORIZED` | No approved real-data path, production permissions, credentials, G10 evidence, or live authority authorization found |

Current UI/report labels are: `SYNTHETIC DEVELOPMENT / PROTOTYPE TRACK`, `NOT CLIENT-APPROVED BUILD`, and `NOT PRODUCTION AUTHORIZATION`.

Reusable implementation may be adopted into a signed build only after applicable Stage 2 / Sign-off C authorization and required hardening. Synthetic mechanism completion does not establish contractual authorization, real-data approval, professional approval, production permission, or G10 authorization. No approval timestamp, signature, client responsibility, or real-data path is fabricated.

