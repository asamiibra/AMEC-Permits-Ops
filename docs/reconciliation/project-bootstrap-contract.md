# Project Bootstrap Contract

> **DEMONSTRATION BASELINE — SYNTHETIC DATA — NOT CLIENT APPROVED**

The synthetic configured trigger is `MANUAL_APPROVED_TRIGGER`. This is a clearly marked assumption, not a claim about the client’s actual trigger. `POST /api/reconciliation/project-bootstrap` records initiation, validates or deterministically allocates a `PRJ-YYYY-NNN` number, rejects duplicate reuse, creates the project, creates the standard Synology root and subfolders, applies synthetic templates, creates the controlled Excel projection row, links the municipality simulator application, and records audit events.

Project numbers are exact controlled identifiers. No fuzzy identity matching or silent renumbering is allowed.
