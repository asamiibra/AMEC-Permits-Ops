# Dashboard Contract Template Lineage

Contract creation resolves the configured Dashboard/master-content Contract Template and stores a template snapshot/version. The Contract origin and template snapshot are read back independently, so later template changes do not rewrite an existing Contract. Dashboard V2 and master-content browser tests passed against PostgreSQL.

The official production Contract Template, language, numbering, and approval content remain Owner Content Required. Synthetic template bytes are intentionally labeled synthetic.

