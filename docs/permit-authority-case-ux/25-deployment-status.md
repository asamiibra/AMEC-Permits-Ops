# Deployment status

Local code and browser verification are complete. Deployed SHA provenance is externally blocked, so the final token is `PERMIT_AUTHORITY_CASE_UX_CODE_FROZEN` with `PERMIT_AUTHORITY_CASE_UX_DEPLOYMENT_PROVENANCE_BLOCKED_EXTERNAL`.
