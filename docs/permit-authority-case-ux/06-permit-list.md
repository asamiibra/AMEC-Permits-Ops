# Permit list evidence

`/permits` is the primary Owner portfolio. It uses server-side query parameters, derived lane counts, safe identifier fallback (`Pending`), canonical status projections, and no binary payloads.
