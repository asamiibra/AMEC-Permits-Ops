# Project Details evidence

The workspace composes Project, Property, PropertyOwnership, Party, DocumentVersion, and provenance. Sensitive identifiers are masked by default; no Permit-local detail model is created.
