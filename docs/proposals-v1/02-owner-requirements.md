# Owner contract and current boundary

Status: BLOCKED; implementation incomplete.

The Owner requires the Project 454 source workspace, >=520 source-only draft discovery, exact hierarchy, immutable capture, governed AI evidence, complete Word-like editing and protected Owner revisions. No production merge or Synology writes are authorized.

The earlier missing-document issue is resolved: the Owner explicitly supplied `AMEC-P-D-2026-Q-498 - Rev 01.docx` and a Desktop copy. Its SHA-256 is d383cd55ac370cb6793e3481bd60774be6b711237693b41eec5b699bd1edf485. The filename difference from the original prompt is now authorized by that follow-up.

First remaining blocker: no qualified full-document editor. A concrete browser rendering candidate materially degrades the actual baseline. The self-hosted alternative cannot be evaluated within the available local disk capacity. Do not interpret the tested text-only mutation primitive as the complete editor.

The live-NAS-only exception does not apply: application persistence/UI/AI integration is incomplete, independently of NAS availability.
