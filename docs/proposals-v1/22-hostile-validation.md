# Hostile validation

Tested at primitive level: no source mutation interface; traversal; symlink escape; file replacement during capture; mid-read mutation/retry; unsupported archive retained as bytes; literal filename handling; source size/count limits; excluded numeric projects; duplicate/stale document anchors; XML entities; missing relationships; package traversal; Owner text/format conflict; structural Owner changes; no-op identity; unchanged XML/media/style/header bytes.

Not tested at application level: duplicate durable sync retries, missing-source historical revision dependencies, cross-project HTTP download, unsupported real AI assertions, Q498 fact leakage, AI protected actions, partial generation activation, source-version persistence, full browser editing, supported database qualification and full mandatory regression. All remain open. No empty result set is used as evidence of zero leakage.
