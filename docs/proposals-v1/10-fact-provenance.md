# Fact provenance status

NOT_IMPLEMENTED for V1. Canonical CandidateAssertion, DocumentVersion, ContextSnapshot, IntelligenceCitation and dependencies were identified for reuse. No competing fact ledger/table was introduced.

The package primitive requires a target anchor and XML precondition hash. It does not establish evidence authority. Its module contract explicitly requires callers to authorize, validate evidence and persist immutable revisions before integration.
