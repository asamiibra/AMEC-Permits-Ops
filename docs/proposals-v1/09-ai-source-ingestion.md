# AI integration status

NOT_IMPLEMENTED. No project-specific source content was submitted to a model.

Reuse canonical DocumentIntelligenceService, registered skill runtime, ModelGateway and AzureOpenAIResponsesProvider. Existing gateway rejects real content at backend/app/ai/gateway.py; AIRuntimeBinding also requires real-content=false. New multimodal/source-scoped behavior must explicitly extend the governed path; a second runtime or labeling real client material synthetic is unacceptable.

The connector never infers substantive facts from filenames. Fixture discovery names are routing metadata only. No AI ingestion PASS is claimed.
