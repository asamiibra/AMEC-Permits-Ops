# RBAC negative matrix

Unauthorized list/preview/download, cross-project file identity, forged download route and capability negatives: NOT_RUN. No new endpoint is exposed. Existing principal/capability model is unchanged.

Connector filesystem negative tests are recorded separately and are not substitutes for this gate.
