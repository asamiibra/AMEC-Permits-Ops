# Hard-Coded Authority Scan

Observed authority names and service identifiers are classified as synthetic fixture/defaults in seed data, mock municipality adapters, test contracts, and default route validation. The scan found no evidence that a production ServicePackVersion governs these occurrences because that model is absent. Therefore production defect count for this gate is `UNRESOLVED`, not zero; final GO is blocked until the service-pack/configuration boundary is proven.
