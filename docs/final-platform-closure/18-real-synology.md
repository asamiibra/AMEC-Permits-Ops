# Real Synology

`REAL_SYNOLOGY_VERIFICATION_BLOCKED_EXTERNAL` emitted. The runtime adapter is `MockSynologyAdapter`; health reports a local mock root and `synthetic=true`. No production application identity, least-privilege permission matrix, real write/readback/hash/promotion, recovery, or backup evidence exists. `REAL_SYNOLOGY_VERIFIED` is withheld.
