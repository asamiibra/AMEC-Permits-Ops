# Production Environment Inventory

Production infrastructure identity is not proven in the current repository. The audited runtime is `APP_ENV=TEST`, `SYNTHETIC_ONLY=true`, PostgreSQL-backed for the audit database, and connected to mock Synology/Excel/municipality adapters. No production URL, deployment ID, provider, storage credential source, or environment-scoped secret was asserted.
