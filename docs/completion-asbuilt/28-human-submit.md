# Human Submit

Status: IMPLEMENTED_AND_VERIFIED.

Owner authorization creates a shared `SubmissionAttempt` in `PENDING_EXTERNAL_CONFIRMATION`; it does not call a real authority and exposes no machine final-submit capability.
