# Submission Package

Status: IMPLEMENTED_AND_VERIFIED.

`SubmissionPackage` and `SubmissionPackageItem` are reused. Completion adds an explicit `as_built_baseline_id` pin while preserving the existing approved-design `baseline_id` meaning; a locked manifest hash is immutable.
