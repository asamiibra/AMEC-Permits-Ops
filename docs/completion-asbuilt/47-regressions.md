# Regressions

Status: PROTECTED_AND_REGRESSION_VERIFIED.

The final fresh PostgreSQL suite passed 176 tests; SQLite passed 175 with one pre-existing PostgreSQL-row-locking skip; frontend passed 32 tests; build passed; registry/safety passed. Existing warnings are documented and not Completion failures.
