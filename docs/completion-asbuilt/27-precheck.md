# Completion Precheck

Status: IMPLEMENTED_AND_VERIFIED.

Precheck evaluates baseline approval, requirements, variance disposition, preparation lock, and package lock into shared `SubmissionPrecheckRun` and `SubmissionPrecheckCheck` records. A blocked precheck cannot authorize submission.
