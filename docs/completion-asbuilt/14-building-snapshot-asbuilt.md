# AS_BUILT BuildingSnapshot

Status: IMPLEMENTED_AND_VERIFIED.

`BuildingAsset` and immutable versioned `BuildingSnapshot` records are project-scoped. `AS_BUILT` requires an explicit verifier, stores structured values, source document IDs, assertion IDs, snapshot hash, and supersession identity.
