# Completion Reports

Status: IMPLEMENTED_AND_VERIFIED; production template source is blocked external.

Reports use shared `TemplateVersion` and deterministic `RenderedArtifact` generation with source hashes and pinned baseline/readiness inputs. AI narrative is optional text only; the rendered report is not treated as source truth.
