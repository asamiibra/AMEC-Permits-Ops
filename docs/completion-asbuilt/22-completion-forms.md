# Completion Forms

Status: IMPLEMENTED_AND_VERIFIED; production form source is blocked external.

Completion forms use `MasterContentItem`, `DocumentVersion`, `FormAutomationProfile`, `MappingRelease`, and `FormInstance`. Only an exact `AUTOMATED_USE_READY` profile can produce automated-ready state; current/reference sources remain manual or blocked.
