# Browser regression

The ProposalOps real-stack regression covers register load, Proposal name/Open navigation, direct detail URL, reload, back/forward, Contract route, Related Proposal link, and contextual upload validation. It passes 2/2. The Stage 1 and persona/Issues/Notifications real-stack checks also pass.

The full 10-test real-stack suite is not green because six older permit-shell/accessibility/visual tests expect missing `Permits`, language-switch, and `Role` controls. These are recorded as blockers to an overall browser-suite completion decision.
