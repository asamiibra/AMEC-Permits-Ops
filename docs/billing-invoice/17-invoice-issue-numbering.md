# Invoice Issue and Numbering

Issue is human Owner-only, requires Accepted status, and allocates a configurable atomic reference. The source sample reference is not used as policy; issued references are unique and non-reusable by default.

Status: IMPLEMENTED_AND_VERIFIED
