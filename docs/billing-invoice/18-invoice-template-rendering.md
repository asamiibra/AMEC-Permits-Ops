# Invoice Template and Rendering

Issue pins the selected template version and uses the shared RenderedArtifact runtime. The supplied invoice file was not available in the workspace, so production template fidelity remains external configuration rather than copied sample data.

Status: BLOCKED_EXTERNAL
