# Operations, support, scale, and test data

Go-live setup includes support contacts, incident handling, maintenance windows, backup/restore, retention, handover, volume/concurrency, test documents, expected results, test targets, and safe fallbacks. The registry distinguishes items needed before go-live from optional scale inputs that inform later design.

The admin setup view is a practical checklist, not a gate or paperwork collection.
