# Browser E2E evidence

`frontend/browser-e2e/readiness-drawer.spec.ts` checks the shared trigger on business, About, administration, and permit-stage routes; verifies the customer count, purpose, inputs, outputs, boundary, admin readiness navigation, CSV export, and Arabic RTL/BiDi behavior.

Screenshots from the readiness drawer and consolidated view belong in `artifacts/production-readiness-ui/` when the browser suite is run with screenshot capture enabled.
