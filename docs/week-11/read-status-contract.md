# READ_STATUS contract

`AuthorityStatusObservation` stores raw authority status, configured normalized status, authority reference, repetition, evidence, and source hash. The implementation accepts only the signed simulator status values and does not invent a universal Qatar status ontology.
