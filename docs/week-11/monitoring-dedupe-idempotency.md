# Monitoring dedupe and idempotency

Stable comment IDs are preferred; otherwise a request/reference/text/section hash is used. Week 7 conservative AuthorityEvent dedupe is reused. Re-reading C1 links to the canonical event and does not create a second Finding or Task. Ambiguous source identity remains reviewable rather than silently merged.
