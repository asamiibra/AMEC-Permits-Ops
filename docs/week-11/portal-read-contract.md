# Portal read contract

`PortalReadContract` versions route/section, field keys, status semantics, comment structure, identity assertions, structural fingerprint, and parser version for each automated read operation. The seeded mock-authority contract is `W11-READ-1.0` with adapter `mock-authority-read/W11-1.0`.
