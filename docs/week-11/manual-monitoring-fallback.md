# Manual monitoring fallback

When a read is unavailable, unauthorized, auth-blocked, or drifted, an authorized human records application identity, status, repetition, comments, capture time, evidence artifacts, and `HUMAN_EVIDENCE`. “Checked portal” free text alone is not accepted. The fallback remains read-only and does not grant submit capability.
