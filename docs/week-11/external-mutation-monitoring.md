# External mutation monitoring

Trusted prior state is compared with current portal-field, attachment, and grid fingerprints. Consequential differences create `ExternalMutationObservation` with prior/current values, changed paths, evidence, reconciliation impact, and authorship `EXTERNAL_HUMAN_OR_AUTHORITY`. PermitOps never attributes that change to itself.
