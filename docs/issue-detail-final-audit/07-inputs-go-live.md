# Inputs and Go-Live

Issue Detail is registered as screen S04A with domain-relevant inputs and no Permit-first setup requirement. The readiness drawer resolves `/issues/:issueId` to the Issue Detail definition rather than applying generic Permit setup.
