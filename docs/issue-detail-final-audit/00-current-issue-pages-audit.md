# Current Issue Pages Audit

The reachable Issue families were inventoried from the canonical Finding projection, Issues list, AMEC Work, and linked notifications. Seven seeded types are reachable. Detail is now one ID-addressed route: `/issues/:issueId`, backed by `GET /api/issues/{issue_id}`. `UNCLASSIFIED_ISSUE_TYPES = 0`.
