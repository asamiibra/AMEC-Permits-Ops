# Client versus Definitions

Client and Contact are business-domain records linked through the Proposal forms-driven seam. Dashboard Definitions provide semantic meaning only, such as Client ID, Scope of Work, and area terminology. A Definition is never used as a client value, and client-name entry does not create a Dashboard Definition.

The Proposal configuration projection labels this boundary explicitly as `DASHBOARD_DEFINITION_SEMANTIC_ONLY`.
