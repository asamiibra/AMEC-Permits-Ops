# Zero-tolerance invariants

The isolated acceptance result records zero for automated final submissions, wrong-application actions, critical false-accept escapes, attachment misfiles, silent readback mismatch accepts, unresolved-blocker resubmission escapes, stale package/precheck final-review escapes, trusted drifted parses, stored secrets, and unauthorized professional closures.
