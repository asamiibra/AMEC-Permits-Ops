# G7 — Contract → Permit

Permit remains a human-controlled downstream action on a Contract. The API resolves the canonical project, refuses missing Project identity, refuses a Permit controlled by another Contract, links `PermitApplication.controlling_contract_id`, and returns the existing Permit workspace route. Final municipality submission remains human-only.
