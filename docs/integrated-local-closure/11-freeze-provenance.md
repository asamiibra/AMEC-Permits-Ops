# Freeze provenance

The final freeze requires the closure-only commit, a pushed origin match, `git diff --check`, and a clean detached verification worktree at the pushed commit. The pre-existing Excel fixture must remain outside the commit. Real Synology and Owner/production acceptance are explicitly outside this local evidence scope.

See `artifacts/integrated-local-closure/freeze-provenance.json`.
