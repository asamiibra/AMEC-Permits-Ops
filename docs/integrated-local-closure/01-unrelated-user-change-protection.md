# Unrelated user change protection

`mock-systems/excel/permit_tracker.xlsx` was pre-existing and remained unstaged and excluded from the closure commit. Its synthetic workbook values were preserved. No raw FORME archive or extracted source was added to the repository.

See `artifacts/integrated-local-closure/unrelated-user-change.json`.
