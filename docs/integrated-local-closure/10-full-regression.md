# Full regression

Post-closure results: backend local full suite `228 passed, 14 skipped`; backend PostgreSQL full suite `236 passed, 6 skipped`; live Samba document contract `12 passed`; frontend tests `38 passed`; frontend build passed with the existing Vite chunk-size warning.

See `artifacts/integrated-local-closure/postgresql-results.json`, `samba-results.json`, and `frontend-results.json`.
