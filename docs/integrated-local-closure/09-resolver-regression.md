# Resolver regression

The fresh FORME needs-review masters were absent from the `PERMIT/AVAILABLE` resolver. Existing canonical current resolver and Proposal Template/Checklist/Contract resolver tests remained passing. Fresh FORME rows did not silently bind unrelated consumer purposes.

See `artifacts/integrated-local-closure/resolver-results.json`.
