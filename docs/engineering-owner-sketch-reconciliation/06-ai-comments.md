# AI comments

AI creates `EngineeringAICommentArtifact` with `AI_ASSISTED_DRAFT` status. The artifact pins project, review, deliverable revision, drawing DocumentVersion, prompt version, runtime/model metadata, actor, and generation time. It is downloadable as a draft and cannot change review, professional approval, baseline, authority finding, or authority response state.
