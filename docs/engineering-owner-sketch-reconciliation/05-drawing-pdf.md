# Drawing and PDF evidence

Drawing Review detail resolves renditions by the selected review’s exact `revision_id`. Open and download actions require the exact rendition and return its exact DocumentVersion, hash, and bytes; no latest-file lookup is used. Opening and downloading are audited.
