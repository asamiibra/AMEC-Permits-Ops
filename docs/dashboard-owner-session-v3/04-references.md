# References and numbering

Added Owner-controlled reference-policy reads and updates for FORM, REPORT, ENGINEERING_WORK, and DEFINITION. Prefix and padding are configurable; `renumber_existing: false` is explicit and existing references are never rewritten.

Default deployed policies remain F/R/E/D with four-digit padding. The focused test changed a Report policy and proved an existing `R-7788` remained unchanged.

`DASHBOARD_V3_REFERENCE_POLICY_PASS`
