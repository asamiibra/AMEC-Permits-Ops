# Expected Client Inputs — Preliminary

The existing expected-input preview remains a policy-derived, Proposal-scoped preliminary projection. It records applicability/status and does not become a Requirement Engine or AuthorityCase truth. The Owner can refresh it; final readiness preserves policy ambiguity/staleness as visible warnings or blockers according to the existing safe default.
