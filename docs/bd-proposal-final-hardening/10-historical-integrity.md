# Historical integrity

Accepted revisions remain immutable. Current material fingerprints include Proposal fields, normalized Forms-v2 projection, and source hashes/statuses. The detail projection shows `current_information_changed`; a changed current state cannot silently rewrite an accepted snapshot.

Duplicate unchanged Accept returns `PROPOSAL_ALREADY_ACCEPTED`.
