# Engineering Work Reference Model

Engineering Works use durable E references and optional structured `engineering_metadata` stored on the item and version snapshot. Eligibility remains based on a current verified source version; existing dependency and lineage flows provide revalidation when an engineering source changes. See `engineering-result.json`.
