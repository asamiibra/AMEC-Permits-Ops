# Administration Forms

Administration Forms and Dashboard Forms are two doors into the same canonical Forms library. They share the same API projection, version history, S/N, reference, category, description, Used In, and source behavior. There is no second Forms datastore. See `admin-forms-result.json`.
