# Cross-Module Propagation

Propagation uses the protected `MasterContentChangeEvent`, explicit dependency registry, `LineageEdge`, and delivery dedupe ledger. Used In controls availability; it does not copy master bytes. Source, category, Used In, and Engineering Work changes are material under the v2 rules. See `cross-module-result.json`.
