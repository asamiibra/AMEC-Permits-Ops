# Reports Connection

Reports are canonical `MasterContentItem` rows with `content_type=REPORT`. Reports consumers resolve the same row/version through the `REPORTS` binding and `REPORT_SOURCE` usage type; no copied report record is created. See `reports-connection-result.json`.
