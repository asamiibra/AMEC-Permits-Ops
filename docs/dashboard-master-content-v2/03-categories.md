# Categories

Categories are seeded as active synthetic/configurable records grouped for Forms, Reports, Engineering Works, and Definitions. Content-type validation is enforced server-side; the category projection exposes `source_kind`, allowed types, and sort order. See `category-seed.json`.
