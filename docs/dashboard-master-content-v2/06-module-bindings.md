# Module Bindings

`MasterContentModuleBinding` is the single binding layer for master content and definitions. Owner-managed PUT routes validate controlled modules and usage types, keep `used_in` as the business projection, and preserve inactive historical bindings. See `module-binding-result.json`.
