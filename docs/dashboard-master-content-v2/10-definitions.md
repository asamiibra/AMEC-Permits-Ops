# Definitions

Definitions are structured records without a required file. They receive D references, retain category and Used In on each `DefinitionRevision`, expose lookup/search, and use the shared module-binding layer with `SEMANTIC_SOURCE`. See `definitions-result.json`.
