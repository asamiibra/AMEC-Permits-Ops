# Schema comparison

Normalized SQLAlchemy inspection compared tables, columns, indexes, unique constraints, and foreign-key targets. Fresh, strict-0051, pre-Construction, and roundtrip databases matched. The As-Built package FK count was one; the 0051 idempotency index and unique constraint were present.
