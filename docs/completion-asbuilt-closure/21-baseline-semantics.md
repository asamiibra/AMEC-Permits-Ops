# Baseline semantics

SubmissionPackageItem.baseline_id remains the approved-design baseline FK. Nullable as_built_baseline_id separately points to as_built_baselines. Package and package-item logic preserve that distinction.
