# E2 template selection policy

Selection is deterministic by artifact type and approved version. Explicit template/version mismatches fail closed. The selected version, content hash, source revisions, and rendering input hash are pinned to every output.
