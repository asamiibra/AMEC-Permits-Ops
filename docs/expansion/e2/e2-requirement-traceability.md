# E2 requirement traceability

Shared template lifecycle → template APIs and migration 0018. Rendering → `/render-requests` and rendered-artifact lineage. Communications → draft/review/approval APIs with `NOT_SENT`. Capabilities → registered definitions and invocation records. UI → reusable status, evidence, authority, draft, and safe-default components.
