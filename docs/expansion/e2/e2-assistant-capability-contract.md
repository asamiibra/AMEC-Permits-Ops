# E2 assistant capability contract

Capabilities are registered metadata with assistant ID, capability version, source classes, requirement IDs, human authority, output types, and execution boundary. Invocation returns a structured candidate/draft envelope and records the invocation.
