# E2 rendered artifact state

Artifacts are `RENDERED` only after deterministic completion. They are never auto-approved or auto-released. Stale or invalidated artifacts must be re-rendered and re-reviewed before downstream use.
