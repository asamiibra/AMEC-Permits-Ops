# E2 shared runtime architecture

Templates, deterministic rendering, communication drafts, capability invocations, workflow tasks, approvals, audit, lineage, and invalidation are shared services over the canonical database. Downstream assistants consume these services; they do not own parallel truth or approval stores.
