# E8 assistant capability fidelity

The capability API and seed registry expose 30+ mapped capabilities across exactly four assistants. Every capability is draft-only, human-review-required, prototype-only, production-disabled, and Stage 2 undecided. E5 engineering and E6 finance/handover remain bounded rather than deferred.
