# E5 compliance and comment rendering

Compliance Review Sheet and Comment Sheet use shared `TemplateDefinition`/`TemplateVersion` and `RenderedArtifact`. Rendered artifacts pin the run, scope, drawing, regulation snapshot, accepted comment set, template, input hash, content hash, and synthetic label. The compliance sheet is explicitly not an approved certificate.
