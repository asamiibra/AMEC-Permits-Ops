# E3 client response contract

Client response evidence is durable, synthetic, and revision-bound. `NO_RESPONSE`, `REQUESTED_CHANGE`, rejection, and expiry do not satisfy acceptance. A change requires a new quotation revision and review.
