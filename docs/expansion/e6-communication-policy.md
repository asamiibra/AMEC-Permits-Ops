# E6 communication policy

Invoice, payment-follow-up, and handover messages are `CommunicationDraft` records with grounded source snapshots and `HUMAN_SEND` policy. Delivery rows remain `NOT_SENT`; there is no send endpoint or external transport.
