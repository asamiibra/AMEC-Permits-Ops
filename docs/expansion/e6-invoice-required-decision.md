# E6 invoice-required decision

Allowed authority sources are `HUMAN_DECISION` and `CONFIGURED_DETERMINISTIC_RULE`. AI inference is rejected. States are `REQUIRED`, `NOT_REQUIRED`, and `NEEDS_REVIEW`; the safe unresolved default is human review.
