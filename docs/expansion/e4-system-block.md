# E4 system block

System blocks expose control code, context, reason, evidence, owner, required action, resolution condition, and lifecycle timestamps. UI/API consumers can explain why setup cannot continue.
