# E5 comment and block time

The block-time projection records opened, assigned, correction-received, re-reviewed, and resolved timestamps plus observed open/blocked durations. It makes no SLA or lateness claim; semantics are `OBSERVED_DURATION_ONLY`.
