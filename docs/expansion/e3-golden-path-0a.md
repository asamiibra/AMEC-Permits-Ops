# Golden Path 0A

RFQ → Opportunity → source intake → candidate fields → human verification → quotation R1 → commercial approval → deterministic render → synthetic release → client acceptance → `READY_FOR_CONTRACT`.
