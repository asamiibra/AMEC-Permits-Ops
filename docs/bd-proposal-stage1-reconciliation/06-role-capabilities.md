# Role capabilities

Business Development creates Proposal intake, registers sources, captures client/contact/request context, owns commercial content and response, and invokes configured commercial transitions. Engineering receives the same Proposal after Proceed, owns technical preparation and Process of Work, and cannot create an arbitrary commercial Proposal or write BD-only price/terms. Owner sees cross-domain context and configured approval capability; visibility does not grant hidden authority. Server-side capability checks remain authoritative.
