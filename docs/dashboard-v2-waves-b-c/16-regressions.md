# Regression verification

Final verification passed: SQLite backend `152 passed, 1 skipped`; PostgreSQL backend `153 passed`; frontend `12 files / 32 tests passed`; frontend production build passed. The real-stack browser selection passed `3` tests, including the legacy V1/V2 route split. The only reported warnings are existing Python deprecation/SQLAlchemy warnings and the existing frontend chunk-size advisory.

Status: `PROTECTED_AND_REGRESSION_VERIFIED`.
