# Wave B — resolver end to end

The automated source resolver requires exact body, service, jurisdiction, and lifecycle context, applies effective dates, and fails closed for missing, ambiguous, or ineligible matches. The synthetic vertical slice verifies the positive exact match and a wrong-context `409` response.

Status: `IMPLEMENTED_AND_VERIFIED`.
