# Security and RBAC

Owner/sponsor roles govern creation and transitions. Process, steward, engineer, and preparer roles can read governed state but cannot release it. No new visible role was introduced. Exact context and release checks are enforced server-side, and the UI only exposes owner actions when the current role allows them.

Status: `PROTECTED_AND_REGRESSION_VERIFIED`.
