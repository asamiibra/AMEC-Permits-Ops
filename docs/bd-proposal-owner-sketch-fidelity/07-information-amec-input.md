# Information / AMEC Input

Additional Information remains Proposal transaction context and is separate from Tender/client conversation notes. AMEC Input remains explicit internal AMEC contribution, rendered with internal visibility copy and retained in the accepted revision snapshot. Existing PATCH audit events record actor, timestamp, field keys, and whether AMEC Input changed; accepted snapshots are immutable and do not silently rewrite historical content.
