# Frontend and build

- Vitest: **32 passed**, 12 test files.
- Vite/TypeScript production build: **passed**.
- The only build notice is the existing large JavaScript chunk warning; it is not a build failure.
- Billing list/detail now shows Contract/Project Context, due state, communication state, and separate communication history without calling an unactivated project a Project Code.
