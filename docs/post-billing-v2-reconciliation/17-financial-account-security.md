# Financial account security

Financial account references are masked in API projections and UI. Issue pins the exact `FinancialAccountVersion`; no bank API or accounting journal is called. Source-safe synthetic account values were used only in tests. No raw account value was added to logs, generic configuration, AI inputs, or evidence.
