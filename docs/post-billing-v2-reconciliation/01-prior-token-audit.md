# Prior token audit

Prior evidence was retained and read; it was not rewritten. The earlier PostgreSQL limitation was treated as a certification gap, not an external dependency.

| Prior token | Result |
|---|---|
| `PREPARATION_SUBMISSION_LOOP_CODE_FROZEN` | Supported by executable evidence; upstream suite remains green. |
| `PERMIT_AUTHORITY_CASE_UX_CODE_FROZEN` | Supported by executable evidence; real authority/Synology remains external. |
| `ADMIN_CONTRACT_OWNER_SKETCH_RECONCILIATION_CODE_FROZEN` | Supported after cleanup-order correction and full PostgreSQL run. |
| `ENGINEERING_OWNER_SKETCH_RECONCILIATION_CODE_FROZEN` | Supported after cleanup-order correction and full PostgreSQL run. |
| `PRE_BILLING_CROSS_WORKSTREAM_BASELINE_CODE_FROZEN` | Reaffirmed after the missing full PostgreSQL proof. |
| `BILLING_INVOICE_CODE_FROZEN` | Reaffirmed after Billing-v2 delta, full PostgreSQL, frontend, and browser evidence. |
| `CONSTRUCTION_POST_APPROVAL_WORKSTREAM_BASELINE_READY` | Reaffirmed as an architecture unlock only; it does not authorize construction. |

The prior Billing and Pre-Billing deployment-provenance limitations remain separate and truthful.
