# SQLite parity

The local SQLite regression passed **177 tests**, skipped **8 PostgreSQL-only tests**, and retained 2 existing warnings. SQLite is parity evidence only; PostgreSQL is the authoritative concurrency/database result.

