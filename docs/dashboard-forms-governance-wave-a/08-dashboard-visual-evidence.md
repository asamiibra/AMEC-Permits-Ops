# Dashboard visual evidence

Local browser smoke check used Vite on `127.0.0.1:5173` with the local API on `127.0.0.1:8000`.

- Forms library heading rendered.
- Advanced governance filters rendered with ownership, artifact kind, currentness, readiness, quality, language, and restricted-sample options.
- `MANUAL USE READY` was present in the readiness selector.
- DOM geometry check: viewport width `1280`, document width `1265`; horizontal overflow `false`.

This is a local synthetic smoke check, not production deployment evidence.
