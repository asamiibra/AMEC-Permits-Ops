# PostgreSQL gate

Final full backend run: **146 passed, 0 failed, 2 warnings, 18.96s** against disposable PostgreSQL `admin_contract_freeze_20260812`, with `APP_ENV=TEST` and `SYNTHETIC_ONLY=true`. Alembic head: `0035_owner_decision_closure`. No SQLite fallback and no skipped required tests.

