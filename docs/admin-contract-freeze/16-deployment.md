# Deployment

The final tested source commit was deployed to the configured frontend/backend targets and parity probes were run after deployment: frontend HTTP 200, backend health green, Alembic `0035_owner_decision_closure`, PostgreSQL durable, synthetic-only behavior explicit, and Synology still not configured. No real Synology verification is claimed.

