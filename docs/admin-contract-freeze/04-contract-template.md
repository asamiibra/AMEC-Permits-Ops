# Contract Template

Administration and the Contract projection resolve the canonical Master Content item, DocumentVersion, version, hash, and rendering lineage. Historical Contracts retain their pinned template version while newer Contracts resolve the current canonical version. The Step 5 artifact strategy remains effective-policy driven with a safe default.

Result: `CONTRACT_TEMPLATE_LIVE_CONNECTION_PRESENT`, `CONTRACT_TEMPLATE_CANONICAL_VERSION_PASS`, `DUPLICATE_CONTRACT_TEMPLATE_TRUTH_ZERO`.

