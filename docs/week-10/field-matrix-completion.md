# Field Matrix completion

`FieldMatrixCoverage` evaluates active critical fields against field authority, verifier, rendering, and municipality configuration. Target-purpose values remain distinct. The endpoint is `GET /api/configuration/field-matrix/coverage`.
