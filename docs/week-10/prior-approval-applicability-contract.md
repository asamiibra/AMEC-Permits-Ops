# Prior approval applicability

Historical approvals are visible but are not silent reuse. A rebuilt package after a prior approved package requires package-specific reapproval; `ApprovalApplicabilityEvaluation` is the deterministic evidence seam for future explicit applicability decisions.
