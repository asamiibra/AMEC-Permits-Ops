# Requirement Matrix completion

The signed synthetic scenario `DEMO_BUILDING_PERMIT_V1` is evaluated by `RequirementMatrixCoverage`. Active requirements are versioned, status-bearing, typed, blocking-aware, and tied to configured document, dependency, field, or human-gate evidence. Unknown is reported explicitly; Week 10 target is `UNKNOWN=0` for the supported scenario.
