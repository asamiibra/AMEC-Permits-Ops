# FindingResolution contract

Resolutions are append-only versions with disposition, correction summary, root cause, corrected entity/version, policy version, proposer, verifier, and correlation ID. Resolution evidence is linked separately. A task can complete without closing its Finding.
