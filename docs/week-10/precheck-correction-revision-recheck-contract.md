# Precheck correction / revision / recheck

R1/PC1 is bound to its exact revision. A material correction marks the historical package, revision, and precheck stale/superseded; a rebuilt package requires reapproval; a new revision pins the new package; PC2 is evaluated independently. Old clearance is never carried forward.
