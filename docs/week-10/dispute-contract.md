# Dispute contract

Disputes require a reason and retain evidence references. Acceptance is policy-controlled. `OTHER_AUTHORITY_COMMENT` is the synthetic code configured with `ALLOWED_FORMAL_DISPUTE`; all other seeded official codes remain `STILL_BLOCKS`. `DISPUTED` alone never bypasses G9.
