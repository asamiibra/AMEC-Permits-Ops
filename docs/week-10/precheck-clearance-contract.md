# Precheck clearance contract

`PrecheckClearanceEvaluation` reports `CLEAR`, `BLOCKED_FINDINGS`, `STALE`, or `NOT_RUN`. Only a current `CLEAR` evaluation on the current revision supports final-review/resubmission readiness.
