# FORME source of truth

The controlling local disposition is the repository’s v1.4 evidence:
23 files plus one empty `CHECK LISTE FORME/MODIFICATION` folder, with 7
`PROMOTE_MASTER_CURRENT`, 7 `PROMOTE_MASTER_NEEDS_REVIEW`, 6 transactional or
historical sources, 1 reference-only source, 2 blocked ambiguous duplicates,
and 1 source gap.

The exact names, references, source paths, SHA-256 values, categories,
Used-In mappings, and review notes are encoded in
`backend/app/fixtures/forme_parity.py` from the supplied Owner mapping and
repository disposition evidence. Only the 14 promoted business identities
are projected; source binaries remain external.

The empty Modification folder remains a source gap. The two byte-identical
Civil Defense / Change Activity observations remain blocked and are not
promoted. No non-master source family is converted into a Dashboard master.
