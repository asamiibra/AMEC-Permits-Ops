# Generic placeholder analysis

`Consultant Form` / `F-0001`: `OBSOLETE_SYNTHETIC_PLACEHOLDER`.

`Authorization Form` / `F-0002`: `OBSOLETE_SYNTHETIC_PLACEHOLDER`.

Both rows are identified by the old `owner-demo-seed` creator, version-one
`owner-demo` source filename, and matching business reference. They have no
purpose binding beyond `AVAILABLE` and no direct master dependency,
applicability, automation, requirement-lineage, or technical-rule-lineage
consumer. They are archived rather than deleted.

`UNCLASSIFIED_GENERIC_PLACEHOLDER_COUNT=0`.
