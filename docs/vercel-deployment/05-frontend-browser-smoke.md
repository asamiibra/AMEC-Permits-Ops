# Frontend browser smoke

Required deployed checks: shell render, synthetic disclosure, Home, seven Business Flow cards, attention queue, Finance and Content Library compact summaries, Recent Business Activity, removed primary-nav labels, role selector, no critical console errors, and no critical network failures. Screenshots and raw results are stored under `artifacts/vercel-deployment/` after the final deployed run.
