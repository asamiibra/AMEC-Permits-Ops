# Upstream Regressions

The complete PostgreSQL backend suite passed 169 tests, including upstream domains. The complete SQLite suite passed 168 tests with only the PostgreSQL-locking skip adjudicated separately. Frontend tests and build passed. The closure made no changes to Completion, As-Built, Handover, Financial Settlement, or external integrations.
