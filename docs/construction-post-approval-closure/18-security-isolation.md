# Security and Isolation

Construction endpoints retain role-based authorization and human-only start authorization. Project-scoped queries and cross-project isolation tests passed. The dirty-tree inventory found no sensitive-data paths; fixture documents and identifiers are marked synthetic. The AI boundary remains advisory/non-authoritative: no AI action can authorize start, send an authority notification, record an authority inspection result, or alter approved design truth.
