# Browser Source-Tree Safety

The Playwright JSON reporter now defaults to `frontend/test-results/`, which is ignored. The Construction screenshot is written to `artifacts/construction-post-approval-closure/`. Before and after the authoritative targeted browser run, no root `test-results` existed and no generated master-content files appeared.
