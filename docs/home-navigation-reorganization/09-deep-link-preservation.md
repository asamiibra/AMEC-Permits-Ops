# Deep-link preservation

The route resolver preserves legacy Dashboard V2 redirects, `/work`, content aliases, permit and authority case patterns, notification and issue detail paths, readiness paths, admin paths, and existing commercial, engineering, construction, completion, and handover paths.

No old route is silently repointed to a new data model. Owner-facing labels change where requested; canonical route ownership remains stable.
