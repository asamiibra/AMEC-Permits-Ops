# Home workspace

Home is a workspace entry surface, not a CRUD module. It contains the seven stage cards, a parallel Finance lane, AMEC Work summary, Issues summary, and a truthful note that recent activity remains in canonical workspaces. Counts are shown only when returned by canonical APIs; errors remain visible rather than becoming synthetic zeroes.
