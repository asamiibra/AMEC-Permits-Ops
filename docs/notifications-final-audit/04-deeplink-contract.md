# Deep-link contract

`NOTIFICATION_DEEP_LINK_REGISTRY` is the canonical event-to-destination map. A destination carries `notification=<id>` and the return filter. The destination workspace reloads the event, shows an Opened from notification context banner, provides Back to Notifications, and remains coherent after hard refresh.
