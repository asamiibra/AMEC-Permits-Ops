# KPIs and filters

`/api/notifications/summary` and `/api/notifications` use the same visible row projection. Unread is the durable read-state count; category cards count visible event categories. Filters use business-readable labels and do not replace the event stream with issue or task data.
