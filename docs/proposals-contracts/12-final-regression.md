# Final regression scope

Regression covers backend compatibility routes, separate register payloads and stable URLs, shared KPI reconciliation, persona boundaries, manual SOR read-back, version lineage, same-project Contract/Permit linking, no cross-project writes, and browser rendering of the two tabs. Production deployment and Neon validation are not claimed by the local synthetic run.
