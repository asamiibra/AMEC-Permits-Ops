# Route/domain reconciliation

`/permits` is a compatibility alias for the commercial collection and redirects to `/proposals-contracts?view=proposals`. Nested `/permits/{projectId}/{stage}` routes remain true downstream Permit Workspace routes. The commercial register also supports `/proposals-contracts?view=contracts`; lifecycle context is preserved through Project identity rather than by treating the commercial collection route as the Permit workspace.
