# ApplicableRuleSet Contract

Each readiness evaluation records scenario version, package-definition version, preparation revision (when present), evaluated system version, input snapshot hash, result hash, and status. Historical evaluations are retained; later configuration releases do not rewrite an earlier snapshot.
