# Minimum Package Definition v1

`MinimumPackageDefinition(PKG-1.0)` defines required critical fields, title deed/owner ID/drawing evidence, dependency rules, drawing controls, human gates, conflict policy, and the package approver role. It is configuration, not a package instance.
