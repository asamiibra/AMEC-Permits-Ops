# Precheck Capture Contract

Synthetic authority precheck runs are bound to one PreparationRevision, retain an evidence artifact reference, result hash, source `AUTHORITY_PRECHECK`, and identifiable items. A later material revision does not silently inherit the prior run. Finding conversion is deferred to Week 7/10.
