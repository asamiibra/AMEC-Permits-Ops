# Requirement Configuration v1

Requirements remain inspectable configuration under the seeded `DEMO_BUILDING_PERMIT_V1` scenario. Applicability is structured JSON; required fields, document types, dependencies, blocking behavior, and human decisions are evaluated without a general-purpose rules language. `MinimumPackageDefinition.version = PKG-1.0` is pinned into every readiness evaluation.
