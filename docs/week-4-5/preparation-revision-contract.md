# PreparationRevision Contract

An approved Package creates a numbered `PreparationRevision` bound to application, package manifest hash, scenario, field-authority, requirement, and rendering versions. Statuses include `READY_FOR_ASSISTED_PREPARATION`, `IN_PREPARATION`, `VERIFIED_DRAFT`, `STALE`, and `SUPERSEDED`.
