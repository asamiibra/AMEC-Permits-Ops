# PreparationSnapshot Contract

The snapshot freezes verified field values, rendered target values, stable grid rows, exact attachment manifest, dependency state, and human-gate state with a deterministic hash. The operator surface works from this snapshot rather than uncontrolled live values.
