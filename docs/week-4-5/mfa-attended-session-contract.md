# MFA Attended Session Contract

The synthetic baseline records `USER_PLUS_OTP`, attendance required, human attendance confirmation, establishment, and expiry. OTP values are never stored. Session state is an operator handoff signal, not an automation credential.
