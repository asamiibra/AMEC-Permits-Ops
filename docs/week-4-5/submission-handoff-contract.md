# Submission Handoff Contract

Handoff requires `VERIFIED_DRAFT`, an approved package, a portal snapshot where available, and a named user with `FINAL_SUBMITTER` role. It produces `READY_FOR_FINAL_HUMAN_REVIEW` and states `HUMAN SUBMISSION REQUIRED`. There is no machine submit endpoint.
