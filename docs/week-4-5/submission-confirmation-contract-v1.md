# Submission Confirmation Contract v1

The synthetic Week 5 confirmation path accepts `MACHINE_READ` or evidence-backed `HUMAN_EVIDENCE` only with an evidence artifact reference. It records observed status, request reference, verifier, and no-automation statement. A click without evidence cannot create a confirmed result.
