# Dependency and Validity Contract

Required dependencies are satisfied only when `ApprovalDependency.status = CURRENT` and the validity date has not passed. Professional credentials are explicit records with status and evidence linkage. Missing, expired, revoked, superseded, or needs-review records block when configured as blocking.
