# Responsive and Accessibility Smoke

Production responsive/accessibility smoke is deferred because no new READY deployment exists. Local browser conformance and accessibility checks are included in the final release test commands.

```text
DEPLOYED_RESPONSIVE_SMOKE_PASS=0
DEPLOYED_ACCESSIBILITY_SMOKE_PASS=0
```
