# Dashboard Invoice Template

Invoice template ownership follows the existing Dashboard/master-content resolver when an eligible production purpose is present. Invoice artifacts retain exact template/version/renderer lineage. Official Invoice Template and financial-account production approval remain Owner-dependent; synthetic fixtures are not promoted as production evidence.
