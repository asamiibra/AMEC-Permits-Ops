# Contracted services

ServiceEngagement remains the canonical service-scope bridge for an activated Project and Contract. Multiple engagements are supported where the configured scope supports them. The Administration landing does not infer services from a Contract name or create a second service subsystem.
