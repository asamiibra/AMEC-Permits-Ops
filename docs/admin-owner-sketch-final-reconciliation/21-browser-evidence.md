# Browser evidence

The real-stack browser spec `frontend/browser-real-stack/admin-owner-ready.spec.ts` covers primary Administration workspaces, backend-derived Contract lanes, secondary Setup & Controls navigation, setting persistence, role denial, narrow viewport behavior, and direct route use. It passed 5/5 against the current frontend and backend on synthetic SQLite storage, with test-support cleanup passing. The adjacent Administration audit and canonical Forms reuse specs also passed 5/5. PostgreSQL/Docker evidence remains unclaimed because Docker is unavailable in this environment.
