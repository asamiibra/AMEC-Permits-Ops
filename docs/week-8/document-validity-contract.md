# Document Validity Contract

`DocumentValidity` evaluates effective dates, expiry, revocation metadata, approval state, and supersession. Required evidence is valid only when its status is `VALID`; current storage status alone is insufficient. Expiry, revocation, and supersession produce an auditable material impact.
