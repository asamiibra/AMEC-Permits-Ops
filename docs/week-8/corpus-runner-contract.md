# Corpus Runner Contract

`CorpusRun`, `CorpusCase`, and `CorpusCaseResult` provide deterministic synthetic regression metrics for classification agreement, critical candidate agreement, verified-final agreement, false accepts, degraded keyed entry, human corrections, evidence quality, and timing fields. Runs are labeled synthetic/non-contractual.
