# Revalidation Contract

Supported bounded actions include readiness re-evaluation, precheck recheck marking, rendering/rebuild requests, new-revision requests, and re-approval requests. The service records the request and human gate; it does not submit, approve, close, or declare on behalf of a person.
