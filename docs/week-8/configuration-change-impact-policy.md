# Configuration Change Impact Policy

Configuration policy rows define severity, whether re-evaluation is required, and whether a new revision is required. Requirement-set and scenario changes require a new preparation revision; rendering and field-authority changes require re-evaluation and stale marking. Historical revisions keep their pinned versions.
