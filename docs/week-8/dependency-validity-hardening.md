# Dependency Validity Hardening

`AuthorityApprovalValidity` mirrors the authority dependency validity window and status. Expired, revoked, superseded, or unknown dependencies block readiness and invalidate downstream package/revision/precheck outputs. The original dependency record is preserved.
