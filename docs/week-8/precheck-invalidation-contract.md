# Precheck Invalidation Contract

Every AuthorityPrecheckRun remains tied to one PreparationRevision. A material change in any upstream document, verified assertion, dependency, credential, or pinned configuration marks the run stale or requires revalidation. Prior clearance is never silently reused.
