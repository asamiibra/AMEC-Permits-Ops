# UI copy and modal polish

Source cards use business-specific helper copy. Focused source cards emphasize the primary input and expose “Add another source.” Proposal creation uses `Create Proposal & Save Sources`; contextual forms use `Upload Proposal Form` or `Upload Contract Form`; Client List uses `Save Client Source`. File names, replace/remove controls, busy state, and read-back success are visible.
