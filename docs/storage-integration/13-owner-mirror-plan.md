# Synology mirror plan

If Virtual DSM or a physical test appliance is available, reproduce the
relevant auth mode, SMB security floor, share ACLs, namespace behavior and
locking semantics. Run the exact provider contract suite and document deltas.
Integration-contract parity is the target; physical disk/RAID identity is not.
