# Building and case subject

The repository has no canonical `BuildingAsset` model. Current scope does not require a building-level case, so no fake building identity was invented and building numbers are not flattened into Property truth. `AuthorityCaseSubject` supports Project and Property with immutable subject snapshots and project-scope validation. BuildingAsset support is an explicit future Owner/domain decision.
