# Week 9 repeating-grid hardening matrix

Independent execution covers reorder, missing, duplicate key, parent mismatch, stable row identity, save/reopen persistence, and mismatch evidence. The current supported grids are `buildings` and `floors`; the bidirectional inventory is in `artifacts/pre-g10-supported-coverage.json`.
