# Repeating Row Identity Contract

Building, floor, and unit rows retain canonical row ID, parent identity, business key, target values, source version, and a deterministic row hash. Portal row IDs are observations, never replacements for canonical identity.
