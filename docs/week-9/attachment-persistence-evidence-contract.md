# Attachment Persistence Evidence Contract

`AttachmentPersistenceEvidence` records expected/observed filename, size, category, source version, save/reopen hashes, capture method, evidence artifact, and result. “Upload succeeded” is never treated as persistence proof.
