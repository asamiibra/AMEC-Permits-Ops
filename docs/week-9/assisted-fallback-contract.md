# Assisted Fallback Contract

When automated reads are unavailable or structure drifts, the operator captures scoped simulator or human evidence. The fallback remains attended and auditable; it does not authorize machine submission or consequential autonomous judgment.
