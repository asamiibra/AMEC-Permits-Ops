# Grid Matching Contract

Rows match by configured business key, not display position. Missing, extra, duplicate-key, ambiguous, and parent-mismatch states are distinct. Reordering alone does not create a mismatch.
