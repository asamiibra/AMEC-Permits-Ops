# Portal Structure Drift Contract

Attachment-tree, grid-schema, and field-schema fingerprints compare expected and observed structure. Drift is persisted, audited, and surfaced as assisted/manual fallback; unknown categories are never auto-mapped.
