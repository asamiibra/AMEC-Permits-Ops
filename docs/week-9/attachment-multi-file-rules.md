# Attachment Multi-File Rules

Category rules support minimum/maximum counts, zero-to-one, one-to-one, and multi-file categories. Reuse is explicit and each category association remains a distinct manifest item, even when the source hash is shared.
