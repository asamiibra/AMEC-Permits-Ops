# Week 9 repeating-grid identity contract

Canonical compatibility path to `repeating-row-identity-contract.md`. Building identity uses `building_ref`; floor identity uses `building_ref + floor_ref`; parent identity remains explicit. Row position is not a consequential identity key.
