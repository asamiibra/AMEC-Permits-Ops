# Parent/Child Grid Integrity

Floor keys include their building; unit keys include building and floor. A child observed under a different parent is `PARENT_MISMATCH`; orphan and duplicate identities are not silently accepted.
