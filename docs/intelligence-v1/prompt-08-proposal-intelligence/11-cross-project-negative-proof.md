# Cross-project negative proof

The Proposal compiler rejects a Proposal source whose entity identity differs from the request scope and rejects non-Proposal scope requests. The runtime constructs the target and source selector server-side from the canonical Proposal.
