# P07 predecessor proof

`git ls-remote` matched the required P07 seal exactly. Parent, tree, P07 manifest (27 entries), architecture SHA256, and Alembic head were independently read back before implementation. `P08_INDEPENDENT_CLONE=true`; shared worktree administration was false.
