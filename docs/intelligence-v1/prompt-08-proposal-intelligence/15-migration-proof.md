# Migration proof

Disposable PostgreSQL `postgres:16` reached PostgreSQL 16.15 and Alembic `p08_proposal_intelligence (head)` from an empty database. The additive migration creates the Proposal review binding table and indexes; there is one head.
