# Proposal context contract

The Proposal context builder resolves the canonical `Opportunity`, exactly one accepted `ProposalAcceptedRevision`, and the revision-scoped `ProposalLpoReconciliation` when present. The shared compiler captures a composite revision identity (`id:number:content_hash`) and emits only bounded projections.
