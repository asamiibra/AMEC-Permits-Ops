# Prompt 04 focused test report

The focused suite is:

```text
COMMAND=PYTHONPATH=. pytest -q backend/tests/test_context_compiler.py
RESULT=27 passed, 1 warning in 57.94s
```

The matrix covers server-derived authorization, manifest gates, all registered resolver types, candidate trust/currentness, exact candidate dependency identity, governed evidence lineage, VerifiedAssertion consumption-only, Master Content exact/purpose resolution and ambiguity, DefinitionRevision, Project projection, source order determinism, snapshot/dependency idempotency, optional omission identity, cross-project isolation, source-specific restricted capability, privacy rejection, atomic rollback, and no authority side effects.
