# Module review primitive

`IntelligenceReviewDecision` is a shared immutable decision ledger. Reviewer identity and persona are derived from the authenticated principal, capability is checked server-side, and promotion requires an explicit human decision. No shared queue, task, or notification entity was created.
