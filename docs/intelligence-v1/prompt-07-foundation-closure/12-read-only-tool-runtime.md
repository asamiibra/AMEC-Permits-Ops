# Read-only tool runtime

`ReadOnlyToolRuntime` accepts only server-registered read-only contracts, checks module/skill/capability and classification ceilings, enforces bounded input/output schemas and timeout, and records each invocation. Mutation-like tool IDs and model-selected loops are rejected/disabled.
