# Tool policy proof

Executed/static: tools are deny-by-default; only server-registered exact `tool_id@version` identities can be declared; client requests carry no tool definitions; P05 rejects enabled tools at the gateway because broad function calling is intentionally deferred. No shell, Python, SQL, browser, payment, signature, submission, approval, or canonical mutation tool is registered.
