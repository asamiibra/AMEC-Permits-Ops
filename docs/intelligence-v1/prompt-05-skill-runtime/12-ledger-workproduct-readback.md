# Ledger/work-product readback

Executed: the focused runtime test read back one ledger, one exact-snapshot-bound `AIWorkProduct`, one exact skill binding, one citation, and the P04 dependency rows in a new session. Provider and validation failures persist no successful work product or citations and finalize the reservation as failed.
