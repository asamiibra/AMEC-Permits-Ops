# Authority negative matrix

Static/verified by implementation: the registry rejects non-`NONE` canonical/protected authority; the runtime imports no canonical mutators, protected commands, review queues, workflow task creators, notifications, submission, payment, signing, approval, release, activation, or close actions. The returned result is explicitly non-authoritative and requires module-owned human review.
