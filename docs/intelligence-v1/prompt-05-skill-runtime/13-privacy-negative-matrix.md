# Privacy negative matrix

Static/verified by implementation: provider input is not logged or audited; audits contain IDs, hashes, counts, classifications, model identity, and error codes only. No JWT, Authorization header, bearer token, password, raw source payload, complete prompt, or complete provider response is placed in ledger/audit metadata. Context dependency metadata retains only safe identity/provenance fields.
