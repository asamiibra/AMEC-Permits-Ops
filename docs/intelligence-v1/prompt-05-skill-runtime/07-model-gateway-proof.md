# Model Gateway proof

Executed: `ModelGateway` is the single provider boundary. It preserves the commissioned Azure binding, managed identity transport, strict server-owned schema selection, empty tools, bounded output, and D4/synthetic-only gates. Manifest model policy is limited to `D4_COMMISSIONED` plus optional exact binding constraints; arbitrary provider, deployment, model, version, region, or deployment type selection is rejected.
