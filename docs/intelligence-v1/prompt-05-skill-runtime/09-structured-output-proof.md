# Structured output proof

Executed: `StructuredOutputDefinition` binds each registry entry to a server-side strict JSON schema and validator. The D3 adapter retains `TechnicalMethodologyDraft`, `extra=forbid`, citation/enum/bounds rules, and deterministic hashing. Generalized execution rejects validator failures and output over 64 KiB before work-product persistence.
