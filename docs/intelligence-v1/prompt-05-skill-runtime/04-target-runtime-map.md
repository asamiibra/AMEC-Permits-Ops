# Target runtime map

`Module request → exact SkillManifest → server authorization → P04 compile_context → snapshot/dependency revalidation → committed ledger reservation → bounded provider input → ModelGateway → strict registered validator → context-bound citations → AIWorkProduct and citations in one transaction → ledger success → non-authoritative response`.

The runtime has no central review queue and performs no canonical or protected business action.
