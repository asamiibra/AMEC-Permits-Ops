# Citation proof

Executed: citation keys are mapped only to ordered P04 compiled items. The runtime persists source type, exact source ID, exact dependency version/hash, snapshot/key locator metadata, and deterministic citation hash. Unknown keys fail; duplicate output references collapse deterministically to one ordinal; work-product and ledger citation counts are set from the persisted set.
