# Regression

After the Vercel-specific changes, the isolated PostgreSQL backend suite passed 236 tests with 6 skips; frontend Vitest passed 38 tests; and the frontend production build passed. Changed Python files passed `py_compile`.

Pre-existing `mock-systems/excel/permit_tracker.xlsx` remained untouched by the certification work and remains an unrelated dirty working-tree file.
