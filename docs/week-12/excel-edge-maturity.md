# Excel edge maturity

Excel projections retain workbook/tab/row identity, preserve human-owned regions, and write only system-owned projection regions. The variant adds configuration-driven owner/company mapping; null handling, language, numeric/date formatting, idempotent projection, and lock failure remain deterministic.
