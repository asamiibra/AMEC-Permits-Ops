# Variant compatibility report

Result: `CONFIGURATION_ONLY`; `domain_schema_change_required=false`; `core_code_fork_required=false`. Differences are owner applicability, `OWNER.CR_NUMBER`, company-registration requirement/attachment, rendering version pins, and a named human applicability decision. The shared Project, Package, PreparationRevision, attachment, grid, finding, closure, and handoff controls remain the same.
