# Client, party, and site contract

Client and Proposal Contact remain distinct from Party master identity. Site / Property context is a Proposal-scoped context and is not silently promoted to Project or AuthorityCase. Legacy `LEGACY_UNSPECIFIED` renders as `Not specified`; the stored value remains compatible for migration.
