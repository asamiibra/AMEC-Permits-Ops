# Client Response and Commercial Outcome

Client Response is available only at `ACCEPTED` / `CLIENT_RESPONSE_PENDING`. Commercial Outcome is available only at `CLIENT_RESPONSE_PENDING`. Both commands are server-gated and require an explicit selected value; no default `PENDING` or `WON` action is rendered.
