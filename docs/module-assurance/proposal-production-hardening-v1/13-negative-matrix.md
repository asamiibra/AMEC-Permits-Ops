# Negative matrix

Implemented negative coverage includes canonical-client absence, caller actor spoofing, mock storage outside TEST, synthetic evidence boundary, exact revision/evidence requirements, server-side LPO field absence/mismatch, failed distribution handoff, stale revision release, and idempotency scope mismatch. The full available backend regression passes 909 tests with 33 existing environment-gated skips; heavyweight source-preflight CLI execution was terminated by the runner and is not claimed as PASS.
