# Storage proof

Provider-neutral verified storage is used for production-shaped Proposal sources and outputs; mock storage is now rejected in AZURE-PREPROD/PROD or whenever synthetic-only is false. DocumentVersion readback/hash/size checks are enforced. DSM/SOR credentials and a real external readback remain `BLOCKED_EXTERNAL`.
