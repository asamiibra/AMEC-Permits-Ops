# Document follow-up and contact routing

Open required client inputs remain canonical ContractClientInputRequirement
rows. Operations exposes missing-input counts and emits the equivalent
`CONTACT_RESOLUTION_REQUIRED` control when a ServiceEngagement has no active
client contact. No unrelated generic number is substituted. Purpose-specific
contact policy confirmation remains an Owner/UAT dependency.

RESULT=PARTIAL
