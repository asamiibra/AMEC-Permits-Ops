# Open Product Decisions

1. Whether OfficialFormVersion should remain Source18-owned metadata on `DocumentVersion` or gain a typed read-only projection for Content Library discovery. No duplicate authority model is authorized pending this decision.
2. Which controlled owner supplies and verifies real current official authority sources for each jurisdiction. Synthetic evidence cannot answer this.
3. Whether/when AI assistance is enabled. Until an explicit policy, the current disabled/fail-closed behavior remains accepted.
4. Whether AMEC-owned proposal/contract template profiles need a formally named product configuration type. Current frozen-purpose behavior is sufficient for this run.

These are product/policy decisions, not unaddressed evidenced Content Library implementation gaps. They block broader product claims only; they do not authorize mutation of the integration target or release branch.
