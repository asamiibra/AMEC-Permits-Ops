# AMEC Phase4 V3.5R1 Revalidation

This candidate is created on the dedicated local branch `phase4-v35-authority-closure-v1` from exact V3.4 SHA `7cb840119237349c58bcbc7ad08380f0db3a85cc`. The preserved historical branch `phase4-corpus-app-integration-v1` remains untouched at `237b4dd23b777e8b375e5a8bbe862090dfd64924`.

The bounded repair closes server-bound review scope, local non-production actor derivation, payload-sensitive idempotency, per-envelope PostgreSQL locking and record-version advancement, correction validation, exact relationship-candidate binding, scoped queue isolation, honest frontend scope/persona/provenance behavior, correction editing, and uncertain-retry identity reuse. The router change only passes existing scope query parameters to the service; no endpoint, schema, model, or migration is added.

The candidate recomputes the exact 120-cell, PIP-001..080, GOV-001..050, and PG-01..30 evidence. The 20 golden paths are bound to executable repository test nodes and raw final-SHA handoff logs; generated PASS labels are not used as their proof. Protected human authorities remain denied, Owner decisions remain pending, and review remains distinct from verification, approval, authorization, and execution.

Normative review-contract/schema bytes and frontend dependency metadata remain byte-identical to V3.4. No Phase3C, Step3A4C, migration, model, deployment, Phase5, Azure, Entra, Synology, SMB, live-shadow, auto-promotion, writeback, or real-AMEC operation is performed. Final commit/tree identity is recorded only in the external immutable handoff.
