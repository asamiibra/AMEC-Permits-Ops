# AMEC Owner Decision Ledger v1

OD-001 through OD-010 use exact questions and `OWNER_DECISION_PENDING`. Each record contains mechanically derived assertion and coverage reverse edges, exact affected domains, current evidence, downstream blocker, and `default=NONE`.
