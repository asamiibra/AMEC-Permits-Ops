# Safe defaults

Proposed defaults are visible recommendations only. They do not set `OWNER_CONFIRMED`, do not become effective runtime values, and do not close a blocking gate. The final untouched register measured `48` proposed-default items, `1` unanswered P0 signoff, `1` external technical block, and `0` confirmed decisions. Apply failures are recorded as `APPLY_FAILED`; they are never reported as applied.
