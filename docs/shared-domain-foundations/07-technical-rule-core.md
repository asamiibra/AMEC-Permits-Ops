# Technical Rule Foundation

Implemented TechnicalRuleSetVersion, TechnicalRule, source lineage, professional approval metadata, active resolver, historical version pinning, unit conversion with incompatible-unit UNKNOWN, deterministic PASS/FAIL/UNKNOWN evaluation, RBAC, and audit.

Activation requires source lineage and responsible professional approval metadata. Active rule sets are immutable and resolution fails closed when absent or ambiguous.
