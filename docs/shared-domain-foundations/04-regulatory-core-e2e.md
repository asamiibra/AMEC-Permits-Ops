# Regulatory Core E2E

The synthetic vertical slice creates a jurisdiction, authority, service/version, lifecycle phase, journey, case, late identifier, work period, outcome, relation, and verifies denied Business Development writes. The same contracts were smoke-tested through the running Postgres-backed stack.

Result: `VERIFIED` in `backend/tests/test_shared_domain_foundations.py` and the real-stack browser contract test.
