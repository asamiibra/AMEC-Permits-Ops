# Requirement Engine E2E

The synthetic slice proves source lineage, activation, exact-context resolution, SATISFIED evaluation, APPLICABILITY_UNKNOWN, wrong-context MISSING, and denied non-approver activation. Ambiguous/no-policy fail-closed behavior is covered by the service resolver contract and API conflict responses.

Result: `VERIFIED`.
