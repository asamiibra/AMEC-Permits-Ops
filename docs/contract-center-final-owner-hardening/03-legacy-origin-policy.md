# Legacy origin policy

The safe default is REQUIRE_ACCEPTED_PROPOSAL. ALLOW_AUTHORIZED_STANDALONE and ALLOW_LEGACY_EXCEPTION remain explicit runtime policy values; no Owner policy is inferred. SYN-CTR-0001 remains a separate unresolved-origin fixture and cannot be accepted under the default.
