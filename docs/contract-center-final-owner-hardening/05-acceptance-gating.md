# Acceptance gating

The Owner page has one Accept Contract control. It is disabled when readiness blockers exist, including unresolved origin and missing required template evidence. The API repeats the readiness gate and finalizes only the exact current ContractRevision.
