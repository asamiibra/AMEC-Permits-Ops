# Cross-Project Negative Tests

Three targeted negative/integrity tests pass. They verify project-reference mismatch rejection and capability denial for Stage 1, block promotion of provisional sources into a different project, and block contextual intake without an eligible project context. The full backend suites also pass the SOR unavailable and project mismatch cases.
