# Current-Pass Protection

Protected behavior remains unchanged: one AMEC Work projection, no legacy duplicate dashboard, no governance fetch on the homepage, controlled API errors, project-scoped proposal intake, canonical Contract/Permit lineage, persona boundaries, Administration access control, and no machine final submission.

The only code-level release fixes in this pass are PostgreSQL-safe seed reset ordering/cleanup, dialect-aware health assertions, and a strict browser locator that now targets the valid Stage 1 heading when multiple scoped work cards exist.
