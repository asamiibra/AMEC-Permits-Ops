# Recurrence prevention controls

`FindingPreventionControl` is a versioned internal operating control attached to `FindingCode`. It names the owner role, evidence requirement, and gate where review is required. Controls are seeded as `W13-1.0`; they do not self-modify and do not convert comments into regulations.
