# SupportCase contract

Routes: `POST /api/support/cases`, `GET /api/support/cases`, and `PATCH /api/support/cases/{id}`. Statuses are `OPEN`, `TRIAGED`, `L1_ACTIVE`, `L2_ACTIVE`, `L3_EXTERNAL`, `MITIGATED`, `RESOLVED`, and `CLOSED`. This is an operational evidence seam, not a full ITSM platform.
