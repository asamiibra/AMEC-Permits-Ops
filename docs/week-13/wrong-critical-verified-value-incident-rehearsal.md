# Wrong critical verified value incident rehearsal

The synthetic runner exercised: wrong critical value → P1 `IntegrityIncident` → `WorkflowSafetyHold` blocking automated writes/final review/resubmission → lineage impact assessment → preserved audit/evidence → Responsible Engineer and role notifications → human-only consequence decision boundary → reconciliation evidence → corrective-action/recurrence context → TEST restore and regression.

The system never decides whether Ministry withdrawal, amendment, correction, or clarification is legally/professionally required. Hold release requires explicit evidence through `/api/workflow-safety-holds/{id}/release`.
