# Issues & Notifications

## ROUTE / PERSONAS

`/issues`, `/issues/:issueId`, `/notifications`; all three personas.

## DESIGN CONTRACT

Issues explain blockers, severity, owner, and next action. Notifications are a separate event stream with read state, delivery evidence, and deep links.

## DESIGN PASS / FUNCTION PASS

PASS on current universal crawl, persona route coverage, deep-link network integrity, and API failure handling. Legacy exact headings such as “What is wrong” are stale selector contracts.

## DATA / ROLE / INTEGRATION

Data comes from persona-scoped issue and notification endpoints; deep links open the related current entity without changing workflow state.

## ERROR / LOADING / EMPTY / MOBILE / ACCESSIBILITY

Failure injection returns contextual error and retry. Current persona/mobile coverage passes; broader suite failures are stale expectations where applicable.

## CROSS-PAGE / DEFECTS / EVIDENCE

Evidence: `screenshots/S04*`, `S04D*`, `S05*`, `role-matrix-result.json`, and `api-failure-result.json`.
