# Deep-link regression

The Home browser suite verified preserved direct navigation to `/work`, `/issues`, `/billing`, `/dashboard`, and `/notifications`. Business-stage cards expose exact canonical targets. Attention rows retain the source `deep_link` and use it for their action CTA.

The full Proposals & Contracts regression also verified the canonical nested workspace route `/proposals-contracts/:project_id/project-and-sources`; `/permits` remains the Regulatory collection root.
