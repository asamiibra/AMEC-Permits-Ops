# Cross-module regression

The focused real-stack browser set passed all 11 tests:

- Proposal Register counts, rows, valid opens, reload, not-found recovery, source cards, and filters
- Forms-Driven v2 owner workspace
- Administration navigation, persisted setting, backend-derived lanes, source connections, project-folder setup, role restrictions, and narrow viewport
- Contract owner detail six-surface sketch and narrow viewport

The full backend PostgreSQL suite also passed the Admin/Contract owner-session tests and Proposal Register tests together.
