# Deployment provenance

The implementation is locally verifiable. Real Synology verification and external deployment SHA remain outside this environment and are recorded as blocked external dependencies.
