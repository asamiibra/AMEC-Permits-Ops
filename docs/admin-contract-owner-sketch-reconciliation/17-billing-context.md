# Billing-readiness context

`GET /api/admin/contracts/{id}/billing-context` is read-only and revision-specific. It reports amount/currency, activation, authority, terms, deliverables, client inputs, exact client evidence, and blockers.
