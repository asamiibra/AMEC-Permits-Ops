# Migration

Migration `0045_admin_contract_owner_sketch_reconciliation` is additive, creates the three revision-scoped Contract tables, adds evidence DocumentVersion linkage, and adds optional commercial fields. Existing Invoice and BillingMilestone tables are untouched.
