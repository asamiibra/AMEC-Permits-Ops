# API contract

The existing `/api/admin/contracts` routes remain compatible. Added seams are authority, billing-context, commercial-terms, deliverables, client-inputs, and enriched owner read-model data. The payment-term payload is intentionally not exposed as a BillingMilestone API.
