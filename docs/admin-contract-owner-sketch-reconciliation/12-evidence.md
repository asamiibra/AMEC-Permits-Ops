# Evidence and LPO handling

ContractAdminEvidence now records `source_role` and optional `document_version_id`. LPO, client-document, and executed-contract roles require an exact DocumentVersion. The projection shows safe metadata only.
