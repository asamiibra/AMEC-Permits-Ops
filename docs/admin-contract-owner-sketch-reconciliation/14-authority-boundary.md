# Contract authority boundary

`POST /api/admin/contracts/{id}/authority` is an explicit Owner capability action. Approval marks the current ContractRevision `APPROVED`, changes the Contract to Ready, and explicitly does not mean legal execution, Invoice approval, or Payment.
