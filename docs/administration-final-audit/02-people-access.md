# People & Access

The route uses roles—Owner, Business Development, and Engineering—not personas. Its capability matrix is returned with the user projection from the same backend authorization model used by runtime checks. Synthetic users are read-only examples; production provisioning is an explicit AMEC input.
