# Project & Folder Setup

The page explains provisional Proposal reference → canonical Project Reference → Contract → Permit → project folder. It shows business references, demo scenario, semantic folder mappings, reconciliation status, synthetic template version, and separate root/Excel setup status; raw UUIDs are not primary context.
