# Browser regression

The six Administration browser tests pass: all direct routes and language checks, bounded actions/persistence, existing owner surface/persistence, source connection and project setup projections, direct role denial, and mobile use. The full real-stack run was 19/30 passing; eleven older non-Administration assertions remain listed separately and are not hidden by this audit.

Machine evidence: `artifacts/administration-final-audit/browser-result.json` and `artifacts/master-realignment/administration-final-browser-result.json`.
