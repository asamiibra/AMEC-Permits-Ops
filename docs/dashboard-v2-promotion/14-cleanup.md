# Cleanup

The active frontend no longer contains a V1/V2 `governanceMode` branch or a live Dashboard V2 product render. The only live compatibility references are the two old bookmark paths. The old split test was replaced with single-dashboard and redirect tests. No shared security, storage, canonical data, or governance primitives were deleted.

The pre-existing Excel fixture remains intentionally dirty and unstaged.
