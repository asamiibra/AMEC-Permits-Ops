# Administration Parity

Administration imports the same `CanonicalFormsLibrary` and reads the same `/api/master-content` records. The parity tests compare canonical reference, category, description, status, version, and current version ID; the Owner status tests additionally compare review note and current version state.

No Administration-specific Form storage or version history exists.
