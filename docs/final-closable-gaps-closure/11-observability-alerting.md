# Observability and Alerting

Structured request events now include event, method, path, correlation ID, actor, status, outcome, latency, and entity context. Health exposes sanitized runtime provenance and adapter health exposes no raw secret/path.

Production log sink, alert destination, and on-call routing require Owner/infrastructure configuration; the software hooks are closed.
