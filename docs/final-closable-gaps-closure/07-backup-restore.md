# Backup and Restore

Local PostgreSQL backup/restore-equivalent evidence and data/API smoke checks passed in the certification environment. Backup boundaries and restore instructions are prepared.

A live production backup/restore rehearsal cannot be claimed without the Owner-authorized production database and policy. The exact remaining sub-gate is `OWNER_INPUT_REQUIRED`.
