# Deployment Provenance

Local frontend build passed and runtime exposes release/build/migration provenance. The tested local stack is reproducible from the repository and current migration head.

The public deployment previously observed is TEST/synthetic and does not provide a verified production SHA. Production deployment authorization/current SHA access is `OWNER_INPUT_REQUIRED`; therefore production GO is prohibited.
