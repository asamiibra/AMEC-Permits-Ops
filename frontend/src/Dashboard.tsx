import { useEffect, useMemo, useState } from "react";
import { api } from "./api";
import { CanonicalFormsLibrary } from "./MasterContentForms";

type Category = { id: string; label: string; allowed_content_types: string[] };
type Version = { id: string; version: number; status: string; file_name: string; updated_by: string; updated_at: string; change_reason?: string };
type MasterItem = { id: string; ref: string; content_type: string; title: string; category?: { id: string; label: string } | null; description?: string; version?: number; version_status: string; updated?: string; versions?: Version[] };
type DefinitionRevision = { id: string; revision: number; description: string; changed_by: string; changed_at: string; change_reason?: string; status: string };
type Definition = { id: string; term: string; description?: string; revision?: number; updated?: string; revisions?: DefinitionRevision[] };

const labels: Record<string, string> = { FORM: "Form", REPORT: "Report", ENGINEERING_WORK: "Document" };
const ownerRoles = new Set(["SYSTEM_ADMIN", "OWNER_SPONSOR"]);

export function DashboardPage({ role }: { role: string }) {
  const [items, setItems] = useState<MasterItem[]>([]); const [definitions, setDefinitions] = useState<Definition[]>([]); const [categories, setCategories] = useState<Category[]>([]);
  const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [query, setQuery] = useState(""); const [category, setCategory] = useState(""); const [editor, setEditor] = useState<{ type: string; item?: MasterItem } | null>(null); const [definitionEditor, setDefinitionEditor] = useState<Definition | null | undefined>(undefined); const [history, setHistory] = useState<{ title: string; versions?: Version[]; revisions?: DefinitionRevision[] } | null>(null); const [busy, setBusy] = useState(false);
  const canWrite = ownerRoles.has(role);
  const load = async () => { setLoading(true); setError(""); try { const [master, defs, cats] = await Promise.all([api<MasterItem[]>(`/api/master-content?q=${encodeURIComponent(query)}${category ? `&category_id=${category}` : ""}`), api<Definition[]>(`/api/definitions?q=${encodeURIComponent(query)}`), api<Category[]>("/api/master-content/categories")]); setItems(master); setDefinitions(defs); setCategories(cats); } catch (err) { setError(err instanceof Error ? err.message : "Dashboard data could not be loaded."); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, [query, category]);
  const sections = useMemo(() => [["REPORT", "Reports"], ["ENGINEERING_WORK", "Engineering Works"]] as const, []);
  return <div className="dashboard-page"><div className="dashboard-intro"><div><span className="eyebrow">AMEC · MASTER / REFERENCE CONTENT</span><h2>Dashboard</h2><p>Maintain reusable Forms, Reports, Engineering Works, and Definitions inside ProposalOps.</p></div><div className="dashboard-controls"><input aria-label="Search master content" placeholder="Search title or reference" value={query} onChange={e => setQuery(e.target.value)} /><select aria-label="Filter by category" value={category} onChange={e => setCategory(e.target.value)}><option value="">All categories</option>{categories.map(c => <option value={c.id} key={c.id}>{c.label}</option>)}</select></div></div>{error && <div className="dashboard-error" role="alert"><b>Dashboard unavailable</b><span>{error}</span><button className="button-secondary" onClick={() => void load()}>Retry</button></div>}{loading && <div className="panel dashboard-state" role="status">Loading master content…</div>}{!loading && !error && <><CanonicalFormsLibrary role={role} /><>{sections.map(([type, heading]) => <MasterSection key={type} type={type} heading={heading} items={items.filter(i => i.content_type === type)} canWrite={canWrite} onNew={() => setEditor({ type })} onEdit={item => setEditor({ type, item })} onHistory={async item => { const data = await api<MasterItem>(`/api/master-content/${item.id}`); setHistory({ title: `${item.ref} · ${item.title}`, versions: data.versions }); }} onDownload={item => void downloadMaster(item.id, role)} />)}</><DefinitionSection definitions={definitions} canWrite={canWrite} onNew={() => setDefinitionEditor(null)} onEdit={setDefinitionEditor} onHistory={async item => { const data = await api<Definition>(`/api/definitions/${item.id}`); setHistory({ title: `${item.term} · Definition history`, revisions: data.revisions }); }} /></>}{editor && <MasterEditor type={editor.type} item={editor.item} categories={categories} busy={busy} onClose={() => setEditor(null)} onSave={async form => { setBusy(true); try { await api(editor.item ? `/api/master-content/${editor.item.id}/versions` : "/api/master-content", { method: "POST", body: form, headers: { "Idempotency-Key": crypto.randomUUID(), "X-Source-Surface": "DASHBOARD" } }); setEditor(null); await load(); } catch (err) { setError(err instanceof Error ? err.message : "The change could not be saved."); } finally { setBusy(false); } }} />}{definitionEditor !== undefined && <DefinitionEditor item={definitionEditor || undefined} busy={busy} onClose={() => setDefinitionEditor(undefined)} onSave={async data => { setBusy(true); try { await api(definitionEditor ? `/api/definitions/${definitionEditor.id}/revisions` : "/api/definitions", { method: "POST", body: JSON.stringify(data) }); setDefinitionEditor(undefined); await load(); } catch (err) { setError(err instanceof Error ? err.message : "The definition could not be saved."); } finally { setBusy(false); } }} />}{history && <HistoryDialog history={history} onClose={() => setHistory(null)} />}</div>;
}

function MasterSection({ type, heading, items, canWrite, onNew, onEdit, onHistory, onDownload }: { type: string; heading: string; items: MasterItem[]; canWrite: boolean; onNew: () => void; onEdit: (item: MasterItem) => void; onHistory: (item: MasterItem) => void; onDownload: (item: MasterItem) => void }) { return <section className="panel dashboard-section" data-testid={`dashboard-${type.toLowerCase()}`}><div className="dashboard-section-head"><div><span className="eyebrow">MASTER LIBRARY</span><h3>{heading}</h3></div>{canWrite && <button className="button-primary" onClick={onNew}>New {labels[type]}</button>}</div><div className="dashboard-table-wrap"><table className="dashboard-table"><thead><tr><th>#</th><th>{labels[type]}</th><th>Ref</th><th>Category</th><th>Description</th><th>Version</th><th>Status</th><th>Updated</th><th>Action</th></tr></thead><tbody>{items.map((item, index) => <tr key={item.id}><td>{index + 1}</td><td><b>{item.title}</b></td><td>{item.ref}</td><td>{item.category?.label || "—"}</td><td>{item.description || "—"}</td><td>v{item.version || "—"}</td><td><span className="dashboard-status">{item.version_status}</span></td><td>{item.updated ? new Date(item.updated).toLocaleDateString() : "—"}</td><td className="dashboard-actions"><button onClick={() => onDownload(item)}>Open</button>{canWrite && <button onClick={() => onEdit(item)}>Modify</button>}<button onClick={() => onHistory(item)}>Version History</button></td></tr>)}</tbody></table>{items.length === 0 && <div className="dashboard-empty">No {heading.toLowerCase()} match the current filters.</div>}</div></section>; }

function DefinitionSection({ definitions, canWrite, onNew, onEdit, onHistory }: { definitions: Definition[]; canWrite: boolean; onNew: () => void; onEdit: (item: Definition) => void; onHistory: (item: Definition) => void }) { return <section className="panel dashboard-section" data-testid="dashboard-definitions"><div className="dashboard-section-head"><div><span className="eyebrow">STRUCTURED SEMANTICS</span><h3>Definitions</h3></div>{canWrite && <button className="button-primary" onClick={onNew}>New Definition</button>}</div><div className="dashboard-table-wrap"><table className="dashboard-table"><thead><tr><th>#</th><th>Term / Word</th><th>Description</th><th>Revision</th><th>Updated</th><th>Action</th></tr></thead><tbody>{definitions.map((item, index) => <tr key={item.id}><td>{index + 1}</td><td><b>{item.term}</b></td><td>{item.description || "—"}</td><td>r{item.revision || "—"}</td><td>{item.updated ? new Date(item.updated).toLocaleDateString() : "—"}</td><td className="dashboard-actions">{canWrite && <button onClick={() => onEdit(item)}>Modify</button>}<button onClick={() => onHistory(item)}>Revision History</button></td></tr>)}</tbody></table>{definitions.length === 0 && <div className="dashboard-empty">No definitions match the current filters.</div>}</div></section>; }

function MasterEditor({ type, item, categories, busy, onClose, onSave }: { type: string; item?: MasterItem; categories: Category[]; busy: boolean; onClose: () => void; onSave: (form: FormData) => Promise<void> }) { const [ref, setRef] = useState(item?.ref || ""); const [title, setTitle] = useState(item?.title || ""); const [category, setCategory] = useState(item?.category?.id || ""); const [description, setDescription] = useState(item?.description || ""); const [reason, setReason] = useState(""); const [file, setFile] = useState<File | null>(null); const allowed = categories.filter(c => c.allowed_content_types.includes(type)); const submit = (event: React.FormEvent) => { event.preventDefault(); const form = new FormData(); if (!item) { form.append("content_type", type); form.append("ref", ref); } else { form.append("expected_current_version", String(item.version || 0)); form.append("change_reason", reason); } form.append("title", title); if (category) form.append("category_id", category); form.append("description", description); if (file) form.append("file", file); void onSave(form); }; return <div className="dashboard-modal-backdrop"><form className="dashboard-modal" onSubmit={submit}><div className="dashboard-modal-head"><div><span className="eyebrow">{item ? "MODIFY · NEW VERSION" : "NEW MASTER CONTENT"}</span><h3>{item ? `${item.ref} · ${item.title}` : `New ${labels[type]}`}</h3></div><button type="button" className="modal-close" onClick={onClose}>×</button></div><label>Title / Name<input required value={title} onChange={e => setTitle(e.target.value)} /></label>{!item && <label>Reference<input required placeholder={type === "FORM" ? "F-001" : type === "REPORT" ? "R-001" : "D-001"} value={ref} onChange={e => setRef(e.target.value)} /></label>}<label>Category<select value={category} onChange={e => setCategory(e.target.value)}><option value="">Select category</option>{allowed.map(c => <option value={c.id} key={c.id}>{c.label}</option>)}</select></label><label>Description<textarea value={description} onChange={e => setDescription(e.target.value)} /></label>{item && <label>Change reason<input required value={reason} onChange={e => setReason(e.target.value)} placeholder="Why is this version changing?" /></label>}<label>File{item && <small className="field-help">Leave the file empty for a metadata-only revision.</small>}<input type="file" accept=".pdf,.docx,.doc,.xlsx,.xls,.txt,.csv" required={!item} onChange={e => setFile(e.target.files?.[0] || null)} /></label><div className="dashboard-modal-foot"><button type="button" className="button-secondary" onClick={onClose}>Cancel</button><button className="button-primary" disabled={busy}>{busy ? "Verifying storage…" : item ? "Save new version" : "Save and verify"}</button></div></form></div>; }

function DefinitionEditor({ item, busy, onClose, onSave }: { item?: Definition; busy: boolean; onClose: () => void; onSave: (data: Record<string, unknown>) => Promise<void> }) { const [term, setTerm] = useState(item?.term || ""); const [description, setDescription] = useState(item?.description || ""); const [reason, setReason] = useState(""); return <div className="dashboard-modal-backdrop"><form className="dashboard-modal" onSubmit={e => { e.preventDefault(); void onSave(item ? { term, description, change_reason: reason, expected_revision: item.revision || 0 } : { term, description }); }}><div className="dashboard-modal-head"><div><span className="eyebrow">STRUCTURED BUSINESS SEMANTICS</span><h3>{item ? `Modify · ${item.term}` : "New Definition"}</h3></div><button type="button" className="modal-close" onClick={onClose}>×</button></div><label>Word / Term<input required value={term} onChange={e => setTerm(e.target.value)} /></label><label>Description<textarea required value={description} onChange={e => setDescription(e.target.value)} /></label>{item && <label>Change reason<input required value={reason} onChange={e => setReason(e.target.value)} /></label>}<div className="dashboard-modal-foot"><button type="button" className="button-secondary" onClick={onClose}>Cancel</button><button className="button-primary" disabled={busy}>{busy ? "Saving…" : item ? "Save revision" : "Save definition"}</button></div></form></div>; }

function HistoryDialog({ history, onClose }: { history: { title: string; versions?: Version[]; revisions?: DefinitionRevision[] }; onClose: () => void }) { return <div className="dashboard-modal-backdrop"><section className="dashboard-modal history-modal"><div className="dashboard-modal-head"><div><span className="eyebrow">IMMUTABLE HISTORY</span><h3>{history.title}</h3></div><button className="modal-close" onClick={onClose}>×</button></div><div className="history-list">{history.versions?.map(v => <div className="history-row" key={v.id}><b>v{v.version}</b><span className="dashboard-status">{v.status}</span><span>{v.file_name}</span><small>{v.change_reason || "—"} · {v.updated_by} · {new Date(v.updated_at).toLocaleString()}</small></div>)}{history.revisions?.map(r => <div className="history-row" key={r.id}><b>r{r.revision}</b><span className="dashboard-status">{r.status}</span><span>{r.description}</span><small>{r.change_reason || "—"} · {r.changed_by} · {new Date(r.changed_at).toLocaleString()}</small></div>)}{!history.versions?.length && !history.revisions?.length && <div className="dashboard-empty">No history available.</div>}</div></section></div>; }

async function downloadMaster(itemId: string, role: string) { const response = await fetch(`/api/master-content/${itemId}/download`, { headers: { "X-Dev-Role": role } }); if (!response.ok) throw new Error("The document could not be opened."); const blob = await response.blob(); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = "master-content"; link.click(); URL.revokeObjectURL(url); }
