import { useEffect, useMemo, useState } from "react";
import { api } from "./api";
import { DocumentsPage, ConflictsPage, ConfigurationPage, SpikePage, SubmissionConfirmationPage } from "./Week2";
import { Phase0Page, AdjudicationPage, AnalysisPage, ThresholdsPage, CorpusPage, Tier1Page, Tier2Page, BusinessBaselinePage, DeliveryPage, CloseDecisionPage, BaselinePage, SignoffPage } from "./Week3";
import { PackageReadinessPage, MunicipalityPreparationPage } from "./Week45";
import { FindingsConsolePage } from "./Week7";
import { LineageValidityPage } from "./Week8";
import { AttachmentGridPage } from "./Week9";
import { ReconciliationControls } from "./ReconciliationControls";
import { AdministrationPage, Application, IssuesPage, MyWorkPage, PermitsPage, PermitWorkspacePage, Project, ReviewsPage, NotificationsPage, WorkflowStage, projectWorkflowStage } from "./WorkflowFirst";
import { ExpansionFoundation } from "./ExpansionFoundation";
import { OpportunitiesPage } from "./Opportunities";
import { EngineeringCloseoutPage } from "./EngineeringCloseout";
import { AboutPermitOpsPage } from "./AboutPermitOps";
import { ReadinessDrawer, ReadinessOverviewPage, getScreenDefinition } from "./ProductionReadiness";
import { AmecLogo } from "./AmecLogo";
import { useLocale } from "./i18n";

type Decision = { id:string; category:string; key:string; status:string; notes?:string };
type Raid = { id:string; type:string; title:string; description:string; severity:string; owner:string; status:string; mitigation:string };

const businessNav = [
  { id: "my-work", label: "My Work", icon: "⌂" },
  { id: "opportunities", label: "Opportunities", icon: "◇" },
  { id: "engineering-closeout", label: "Engineering & Closeout", icon: "✦" },
  { id: "permits", label: "Permits", icon: "▦" },
  { id: "reviews", label: "Reviews", icon: "✓" },
  { id: "issues", label: "Issues", icon: "!" },
  { id: "notifications", label: "Notifications", icon: "◌" },
];
const legacyNav = [
  {id:"expansion-foundation",label:"Expansion foundation"},
  {id:"dashboard",label:"Legacy control room"},
  {id:"projects",label:"Project register"},{id:"documents",label:"Documents / source evidence"},{id:"conflicts",label:"Conflicts"},{id:"config",label:"Configuration"},{id:"package",label:"Package readiness"},{id:"municipality",label:"Municipality preparation"},{id:"findings",label:"Findings & work"},{id:"lineage",label:"Lineage & validity"},{id:"attachments-grids",label:"Attachments & grids"},{id:"spike",label:"Test extraction"},{id:"adjudication",label:"Expected results"},{id:"analysis",label:"Test analysis"},{id:"thresholds",label:"Test targets"},{id:"corpus",label:"Test documents"},{id:"tier1",label:"Tier 1 decisions"},{id:"tier2",label:"Tier 2 backlog"},{id:"delivery",label:"Delivery / data"},{id:"close",label:"Go-live setup decision"},{id:"baseline",label:"Setup baseline"},{id:"signoff",label:"Commercial draft"},{id:"confirmation",label:"Submission confirmation"},{id:"discovery",label:"Project setup"},{id:"business",label:"Business case"},{id:"business-baseline",label:"Business baseline"},{id:"privacy",label:"Privacy & data"},{id:"volume",label:"Volume baseline"},{id:"inquiries",label:"Ministry inquiry"},{id:"raid",label:"RAID log"},{id:"control-loop",label:"Control diagnostics"},
];
const adminRoles = new Set(["SYSTEM_ADMIN", "REQUIREMENT_STEWARD", "PORTAL_MAINTAINER"]);
const statusClass = (status:string) => `status status-${status.toLowerCase().replaceAll("_","-")}`;
const pageFromPath = () => {
  const path = window.location.pathname;
  if (path === "/work" || path === "/") return "my-work";
  if (path === "/permits" || path === "/projects") return "permits";
  if (path === "/opportunities") return "opportunities";
  if (path === "/engineering-closeout") return "engineering-closeout";
  if (path === "/reviews") return "reviews";
  if (path === "/issues") return "issues";
  if (path === "/notifications") return "notifications";
  if (path === "/about" || path === "/how-permitops-works") return "about";
  if (path === "/admin") return "administration";
  if (path === "/admin/control-diagnostics") return "control-loop";
  if (path.startsWith("/admin/")) return path.slice("/admin/".length);
  if (path.startsWith("/permits/")) return "permit-workspace";
  return "my-work";
};
const stageFromPath = (): WorkflowStage => {
  const part = window.location.pathname.split("/").pop()?.replaceAll("-", "_").toUpperCase();
  return part && ["PROJECT_AND_SOURCES", "VERIFY_DATA", "PREPARE_PACKAGE", "MUNICIPALITY_PREPARATION", "FINAL_REVIEW", "AUTHORITY_REVIEW", "COMMENTS_AND_CORRECTIONS", "HISTORY"].includes(part) ? part as WorkflowStage : "PROJECT_AND_SOURCES";
};

function App() {
  const { locale } = useLocale();
  const [page,setPage] = useState(pageFromPath); const [selected,setSelected] = useState<Project|null>(null); const [selectedStage,setSelectedStage] = useState<WorkflowStage>(stageFromPath); const [projects,setProjects] = useState<Project[]>([]); const [apps,setApps] = useState<Application[]>([]); const [governance,setGovernance] = useState<any>(null); const [error,setError] = useState(""); const [role,setRole] = useState(() => sessionStorage.getItem("permitops-role") || "PERMIT_PREPARER");
  useEffect(()=>{ Promise.all([api<Project[]>("/api/projects"),api<Application[]>("/api/applications"),api<any>("/api/reconciliation/governance")]).then(([p,a,g])=>{setProjects(p);setApps(a);setGovernance(g); const parts = window.location.pathname.split("/"); if (page === "permit-workspace" && parts[2]) { const project = p.find((item) => item.id === parts[2]); if (project) setSelected(project); } }).catch(e=>setError(e.message)); },[]);
  useEffect(() => { sessionStorage.setItem("permitops-role", role); }, [role]);
  useEffect(() => {
    const syncLocation = () => {
      const nextPage = pageFromPath();
      const projectId = window.location.pathname.startsWith("/permits/") ? window.location.pathname.split("/")[2] : undefined;
      setPage(nextPage);
      setSelectedStage(stageFromPath());
      setSelected(projectId ? projects.find((project) => project.id === projectId) || null : null);
    };
    window.addEventListener("popstate", syncLocation);
    return () => window.removeEventListener("popstate", syncLocation);
  }, [projects]);
  useEffect(() => { if (window.location.pathname.startsWith("/admin") && !adminRoles.has(role)) { setPage("my-work"); window.history.replaceState({}, "", "/work"); } }, [role]);
  const navigate = (next: string) => { setPage(next); setSelected(null); const path = next === "my-work" ? "/work" : next === "administration" ? "/admin" : next === "go-live-readiness" ? "/admin/go-live-readiness" : `/${next}`; window.history.pushState({}, "", path); };
  const openPermit = (projectId: string, stage: WorkflowStage = "PROJECT_AND_SOURCES") => { const project = projects.find((item) => item.id === projectId) || projects[0]; if (!project) return; setSelected(project); setSelectedStage(stage); setPage("permit-workspace"); window.history.pushState({}, "", `/permits/${project.id}/${stage.toLowerCase().replaceAll("_", "-")}`); };
  const openLegacy = (next: string, projectId?: string) => { if (!adminRoles.has(role) && !projectId) { navigate("my-work"); return; } if (next === "control-loop") { setPage("control-loop"); setSelected(null); window.history.pushState({}, "", "/admin/control-diagnostics"); return; } setPage(next); setSelected(projectId ? projects.find((item) => item.id === projectId) || null : null); window.history.pushState({}, "", projectId ? `/permits/${projectId}/${next === "package" ? "package" : next === "municipality" ? "municipality" : next}` : `/admin/${next}`); };
  const openProject=(p:Project)=>openPermit(p.id);
  const visibleBusinessNav = businessNav.filter((item) => {
    if (role === "PERMIT_PREPARER") return ["my-work", "permits", "issues", "notifications"].includes(item.id);
    if (role === "PACKAGE_APPROVER" || role === "FINAL_SUBMITTER") return ["my-work", "permits", "reviews", "notifications"].includes(item.id);
    return true;
  });
  const title = page === "permit-workspace" && selected ? `${selected.project_number} · ${selected.project_name}` : page === "administration" ? "Administration" : page === "go-live-readiness" ? "Go-Live Setup" : (visibleBusinessNav.find((item) => item.id === page)?.label || legacyNav.find((item) => item.id === page)?.label || (page === "project-detail" ? "Project detail" : "PermitOps"));
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><AmecLogo size="sm" className="sidebar-amec-logo"/><div className="brand-product"><b>PermitOps</b><small>PERMIT INTEGRITY &amp; WORKFLOW</small></div></div><div className="office-pill"><span className="dot"/><span>AMEC Engineering</span><br/><small>QEC-DOHA · SYNTHETIC DEV</small></div><nav aria-label="Primary navigation">{visibleBusinessNav.map(item=><button key={item.id} className={page===item.id?"nav-item active":"nav-item"} onClick={()=>navigate(item.id)}><span className="nav-icon">{item.icon}</span>{item.label}</button>)}{adminRoles.has(role) && <><div className="nav-divider"/><button className={page === "administration" ? "nav-item active" : "nav-item"} onClick={()=>navigate("administration")}><span className="nav-icon">⚙</span>Administration</button></>}</nav><button className={page === "about" ? "nav-item active" : "nav-item"} onClick={()=>navigate("about")}><span className="nav-icon">?</span>About PermitOps</button><div className="sidebar-foot"><span className="lock">▣</span><span><b>Safe boundary</b><small>Synthetic data only<br/>No portal writes<br/>No closure automation</small></span></div></aside><main className="main"><header className="topbar"><div className="topbar-heading"><AmecLogo size="sm" className="mobile-topbar-amec-logo"/><div><span className="eyebrow">PERMIT WORKFLOW</span><h1>{title}</h1></div></div><div className="top-actions"><ReadinessDrawer screenId={page === "permit-workspace" ? getScreenDefinition(selectedStage).screenId : getScreenDefinition(page).screenId} role={role} onNavigate={navigate}/><span className="env-chip"><span className="dot green"/> {governance?.environment_badge || "SYNTHETIC PROTOTYPE"}</span><label aria-label="Role" className="role-switcher">Demo as<select value={role} onChange={(event) => setRole(event.target.value)}><option value="PERMIT_PREPARER">Permit Preparer</option><option value="DATA_VERIFIER">Data Verifier</option><option value="RESPONSIBLE_ENGINEER">Responsible Engineer</option><option value="PACKAGE_APPROVER">Package Approver</option><option value="FINAL_SUBMITTER">Final Submitter</option><option value="SYSTEM_ADMIN">System Admin</option></select></label><button className="avatar" aria-label="Current user">SA</button></div></header><div className="content">{error&&<div className="error-banner">API unavailable: {error}. Start the backend to view seeded data.</div>}{page === "administration" || page === "control-loop" || page.startsWith("admin-") || page === "go-live-readiness" || page === "about" ? null : <div className="synthetic-note compact-environment-badge">SYNTHETIC PROTOTYPE · NO PORTAL WRITES · HUMAN SUBMISSION REQUIRED</div>}{page==='my-work'&&<MyWorkPage projects={projects} applications={apps} openPermit={openPermit} openAbout={()=>navigate("about")}/>} {page==='about'&&<AboutPermitOpsPage onNavigate={navigate}/>} {page==='opportunities'&&<OpportunitiesPage/>} {page==='engineering-closeout'&&<EngineeringCloseoutPage/>} {page==='permits'&&<PermitsPage projects={projects} applications={apps} openPermit={openPermit}/>} {page==='reviews'&&<ReviewsPage projects={projects} applications={apps} openPermit={openPermit}/>} {page==='issues'&&<IssuesPage projects={projects} applications={apps} openPermit={openPermit}/>} {page==='notifications'&&<NotificationsPage projects={projects} applications={apps} openPermit={openPermit}/>} {page==='permit-workspace'&&selected&&<PermitWorkspacePage project={selected} application={apps.find((item) => item.project_id === selected.id)} activeStage={selectedStage} openStage={(stage) => { setSelectedStage(stage); window.history.pushState({}, "", `/permits/${selected.id}/${stage.toLowerCase().replaceAll("_", "-")}`); }} openLegacy={openLegacy} backToPermits={() => navigate("permits")}/>} {page==='administration'&&<AdministrationPage openLegacy={openLegacy}/>} {page==='go-live-readiness'&&<ReadinessOverviewPage onNavigate={navigate} role={role}/>} {page==='expansion-foundation'&&<ExpansionFoundation/>} {page==='projects'&&<Projects projects={projects} apps={apps} open={openProject}/>} {page==='project-detail'&&selected&&<ProjectDetail project={selected} apps={apps.filter(a=>a.project_id===selected.id)} back={()=>navigate("permits")}/>} {page==='package'&&<PackageReadinessPage initialProjectId={selected?.id}/>} {page==='municipality'&&<MunicipalityPreparationPage initialProjectId={selected?.id}/>} {page==='findings'&&<FindingsConsolePage/>}{page==='lineage'&&<LineageValidityPage/>}{page==='attachments-grids'&&<AttachmentGridPage/>}{page==='documents'&&<DocumentsPage/>}{page==='conflicts'&&<ConflictsPage/>}{page==='config'&&<ConfigurationPage/>}{page==='spike'&&<SpikePage/>}{page==='adjudication'&&<AdjudicationPage/>}{page==='analysis'&&<AnalysisPage/>}{page==='thresholds'&&<ThresholdsPage/>}{page==='corpus'&&<CorpusPage/>}{page==='tier1'&&<Tier1Page/>}{page==='tier2'&&<Tier2Page/>}{page==='delivery'&&<DeliveryPage/>}{page==='close'&&<CloseDecisionPage/>}{page==='baseline'&&<BaselinePage/>}{page==='signoff'&&<SignoffPage/>}{page==='confirmation'&&<SubmissionConfirmationPage/>}{page==='discovery'&&<Discovery/>}{page==='business'&&<BusinessCase/>}{page==='business-baseline'&&<BusinessBaselinePage/>}{page==='privacy'&&<Privacy/>}{page==='volume'&&<Volume/>}{page==='inquiries'&&<Inquiries/>}{page==='raid'&&<Raid/>}{page==='control-loop'&&<ReconciliationControls state={{packageStatus:"BLOCKED",blockedReasons:["Current evidence requires human approval"],packageStale:true,revisionStale:true,portalMismatch:true,rtl:true,municipalityValue:"Doha",dropdownCode:"MUN_A",dropdownLabel:"Doha Municipality",findingOwner:"Responsible Engineer",taskLabel:"Finding remediation",notificationStatus:"FAILED",precheckRun:"SYN-PRECHECK-0142",precheckRevision:"R2",handoffStatus:"HUMAN SUBMISSION REQUIRED"}}/>}</div></main></div>
}

function PageIntro({kicker,title,description}:{kicker:string;title:string;description:string}){return <div className="page-intro"><div><span className="eyebrow">{kicker}</span><h2>{title}</h2><p>{description}</p></div></div>}
function Dashboard({projects,apps,go}:{projects:Project[];apps:Application[];go:(p:string)=>void}){const returned=apps.filter(a=>a.application_status==='RETURNED').length;return <><PageIntro kicker="WEEK 1 CONTROL ROOM" title="A safe foundation for permit integrity" description="Canonical PermitOps data connected to three synthetic operating surfaces. Every link and decision is auditable."/><div className="metric-grid"><Metric label="Active projects" value={projects.filter(p=>p.status==='ACTIVE').length} tone="blue"/><Metric label="Applications" value={apps.length} tone="teal"/><Metric label="Returned applications" value={returned} tone="orange"/><Metric label="Open RAID items" value="10" tone="red"/></div><div className="two-col"><section className="panel"><div className="panel-head"><div><span className="eyebrow">PORTFOLIO</span><h3>Projects requiring attention</h3></div><button className="text-button" onClick={()=>go("projects")}>View all →</button></div><div className="mini-list">{projects.slice(0,4).map(p=><button key={p.id} onClick={()=>go("projects")}><span><b>{p.project_number}</b><small>{p.project_name}</small></span><span className={statusClass(apps.find(a=>a.project_id===p.id)?.application_status||"DRAFT")}>{apps.find(a=>a.project_id===p.id)?.application_status||"NO APP"}</span></button>)}</div></section><section className="panel discovery-card"><span className="eyebrow">DISCOVERY READINESS</span><h3>Privacy & authority access</h3><div className="progress"><div style={{width:"28%"}}/></div><b>2 of 7 decisions confirmed</b><p>Real sensitive-document processing remains not approved. Review the discovery dashboard before any Phase 0 data decision.</p><button className="button-secondary" onClick={()=>go("discovery")}>Open discovery →</button></section></div><ReconciliationControls state={{packageStatus:"BLOCKED",blockedReasons:["Current evidence requires human approval"],packageStale:true,revisionStale:true,portalMismatch:true,rtl:true,municipalityValue:"Doha",dropdownCode:"MUN_A",dropdownLabel:"Doha Municipality",findingOwner:"Responsible Engineer",taskLabel:"Finding remediation",notificationStatus:"FAILED",precheckRun:"SYN-PRECHECK-0142",precheckRevision:"R2",handoffStatus:"HUMAN SUBMISSION REQUIRED"}}/><div className="synthetic-note">SYNTHETIC DEMONSTRATION DATA — REPLACE DURING PHASE 0 · No real QIDs, title deeds, credentials, or authority connections.</div></>}
function Metric({label,value,tone}:{label:string;value:string|number;tone:string}){return <div className={`metric ${tone}`}><span>{label}</span><strong>{value}</strong><small>Week 1 baseline</small></div>}
function Projects({projects,apps,open}:{projects:Project[];apps:Application[];open:(p:Project)=>void}){return <><PageIntro kicker="CANONICAL REGISTER" title="Projects" description="PermitOps project identity stays canonical while external representations remain explicit links."/><section className="panel table-panel"><div className="toolbar"><span className="search-box">⌕ <input placeholder="Filter projects"/></span><span className="muted">{projects.length} records · synthetic</span></div><table><thead><tr><th>Project #</th><th>Project</th><th>Municipality</th><th>Permit type</th><th>Application #</th><th>Status</th><th>Repeats</th><th>Assigned engineer</th></tr></thead><tbody>{projects.map(p=>{const a=apps.find(x=>x.project_id===p.id);return <tr key={p.id} onClick={()=>open(p)}><td><b className="linkish">{p.project_number}</b></td><td>{p.project_name}</td><td>{p.municipality}</td><td>{p.permit_type}</td><td>{a?.external_request_number||"—"}</td><td><span className={statusClass(a?.application_status||"DRAFT")}>{a?.application_status||"NO APP"}</span></td><td>{a?.repetition_count??"—"}</td><td>{p.assigned_engineer||"—"}</td></tr>})}</tbody></table></section></>}
function ProjectDetail({project,apps,back}:{project:Project;apps:Application[];back:()=>void}){const [detail,setDetail]=useState<any>(null);useEffect(()=>{api<any>(`/api/projects/${project.id}`).then(setDetail).catch(()=>{})},[project.id]);const a=apps[0];return <><button className="back-button" onClick={back}>← Projects</button><PageIntro kicker={project.project_number} title={project.project_name} description={`${project.municipality} · ${project.permit_type} · ${project.workstream||"Permit workstream"}`}/><div className="detail-grid"><section className="panel"><div className="panel-head"><h3>External system links</h3><span className="tag">3 representations</span></div>{(detail?.links||[]).map((l:any)=><div className="link-row" key={l.id}><span className={`system-mark ${l.system_type.toLowerCase()}`}>{l.system_type[0]}</span><span><b>{l.system_type==='MUNICIPALITY'?'Permit Authority Simulator':l.system_type}</b><small>{l.display_reference}</small></span><span className="linked">LINKED</span></div>)}<div className="mismatch">↗ User confirmation is required when identifiers conflict. No fuzzy auto-linking.</div></section><section className="panel"><div className="panel-head"><h3>Application status</h3>{a&&<span className={statusClass(a.application_status)}>{a.application_status}</span>}</div>{a?<><div className="app-number">{a.external_request_number}</div><div className="app-stats"><div><span>Repetition count</span><b>{a.repetition_count}</b></div><div><span>Authority</span><b>Permit Authority Simulator</b></div></div>{a.application_status==='RETURNED'&&<div className="comments"><b>Synthetic authority comments</b><p>• Owner name differs from supporting document.</p><p>• Drawing revision does not match package revision.</p><p>• Required attachment missing.</p></div>}</>:<p>No application linked.</p>}</section></div><ReconciliationFoundation projectId={project.id}/><section className="placeholder-grid">{["Documents","Verified facts","Requirements","Package","Municipality preparation","Findings"].map(t=><div className="placeholder" key={t}><span>{t}</span><b>Planned for subsequent build wave</b><small>Week 1 intentionally does not implement this capability.</small></div>)}</section><section className="panel"><div className="panel-head"><h3>Activity & audit</h3><span className="muted">Correlation IDs retained</span></div>{(detail?.audit||[]).slice(0,5).map((e:any)=><div className="audit-row" key={e.id}><span className="audit-dot"/><span><b>{e.event_type}</b><small>{new Date(e.occurred_at).toLocaleString()} · {e.correlation_id.slice(0,12)}...</small></span></div>)}</section></>}
function ReconciliationFoundation({projectId}:{projectId:string}){const [foundation,setFoundation]=useState<any>(null);const [property,setProperty]=useState<any>(null);const [rendering,setRendering]=useState<any>(null);useEffect(()=>{Promise.all([api<any>(`/api/reconciliation/projects/${projectId}`),api<any>(`/api/reconciliation/properties/${projectId}`),api<any>(`/api/reconciliation/rendering-preview?project_id=${projectId}&field_code=PROPERTY.PLOT_NUMBER`)]).then(([f,p,r])=>{setFoundation(f);setProperty(p);setRendering(r)}).catch(()=>{})},[projectId]);if(!foundation)return null;return <section className="panel reconciliation-foundation"><div className="panel-head"><div><span className="eyebrow">RECORDING-DERIVED FOUNDATION</span><h3>Identity, ownership & rendering provenance</h3></div><span className="tag">{foundation.fixture?.fixture_version}</span></div><div className="reconciliation-grid"><div><b>Initiation</b><small>{foundation.initiation?.initiation_type} · {foundation.initiation?.initiation_reference}</small><b>Number reservation</b><small>{foundation.reservation?.proposed_number} · {foundation.reservation?.status}</small><b>Synology</b><small>{foundation.synology?.root_path}</small><b>Excel</b><small>{foundation.excel?.workbook_identity} · {foundation.excel?.sheet_name} / row {foundation.excel?.row_number}</small></div><div><b>Property</b><small>{property?.property?.pin} · plot {property?.property?.plot_number}</small><b>Owners</b>{(property?.owners||[]).map((o:any)=><small key={o.id}>{o.party?.name_en} · {Math.round(o.normalized_share*100)}%</small>)}<b>Representation</b><small>{property?.representations?.[0]?.representative?.name_en||"None"} · {property?.authorizations?.[0]?.status||"—"}</small><b>Rendering preview</b><small>Raw: {rendering?.raw_observation?.raw_value} · Canonical: {rendering?.canonical_verified_value?.value}</small><small>Targets: {Object.entries(rendering?.target_renderings||{}).map(([k,v]:any)=>`${k}=${v.value}`).join(" · ")}</small></div></div></section>}

function Discovery(){const [items,setItems]=useState<Decision[]>([]);useEffect(()=>{api<Decision[]>("/api/discovery/decisions").then(setItems).catch(()=>{})},[]);const update=(item:Decision)=>{const status=item.status==='UNKNOWN'?'CONFIRMED':'UNKNOWN';api<Decision>(`/api/discovery/decisions/${item.id}`,{method:"PATCH",body:JSON.stringify({status})}).then(x=>setItems(items.map(i=>i.id===x.id?x:i))) };return <><PageIntro kicker="PHASE 0 EVIDENCE BOARD" title="Discovery dashboard" description="Track the decisions that must be confirmed before real data or authority integration is considered."/><div className="decision-grid">{items.map(i=><div className="decision-card" key={i.id}><div className="decision-top"><span className="eyebrow">{i.category}</span><button className={`decision-status ${i.status.toLowerCase()}`} onClick={()=>update(i)}>{i.status}</button></div><h3>{i.key.replaceAll("_"," ")}</h3><p>{i.notes}</p><small>Click status to simulate a reviewed decision. Audit event is created.</small></div>)}</div><div className="two-col"><section className="panel"><span className="eyebrow">PROCESS</span><h3>Current-state journey</h3><p className="journey">Project → documents → manual entry → internal review → simulator preparation → human final submission → authority review</p><span className="tag warning">WORKING HYPOTHESIS — VALIDATE WITH CLIENT</span></section><section className="panel privacy-callout"><span className="eyebrow">DATA SAFETY</span><h3>Development policy</h3><strong>SYNTHETIC / REDACTED ONLY</strong><p>External AI route: DISABLED UNTIL APPROVED. No raw sensitive-document access is authorized.</p></section></div></>}
function BusinessCase(){const [v,setV]=useState<any>(null);useEffect(()=>{api<any>("/api/discovery/business-case").then(setV).catch(()=>{})},[]);if(!v)return <Loading/>;const change=(key:string,value:string)=>{const next={...v,[key]:Number(value)};setV({...v,...next,applications_per_year:next.applications_per_month*12,annual_manual_hours:next.applications_per_month*12*(next.manual_data_entry_minutes+next.upload_minutes+next.status_check_minutes)/60})};return <><PageIntro kicker="ILLUSTRATIVE BASELINE" title="Business-case snapshot" description="Current synthetic baseline exposure. Benefit assumptions will be established after Phase 0 evidence."/><div className="synthetic-note">SYNTHETIC DEMONSTRATION DATA — REPLACE DURING PHASE 0</div><div className="business-layout"><section className="panel inputs"><h3>Editable inputs</h3>{[["applications_per_month","Applications / month"],["manual_data_entry_minutes","Data entry min / application"],["upload_minutes","Upload min / application"],["status_check_minutes","Status checks min / application"],["return_rate","Return rate (decimal)"],["rework_hours_per_return","Rework hours / return"],["delay_days_per_return","Delay days / return"],["loaded_hourly_rate_qar","Loaded labour rate (QAR / hour)"]].map(([k,l])=><label key={k}>{l}<input type="number" value={v[k]} step={k==='return_rate'?0.05:1} onChange={e=>change(k,e.target.value)}/></label>)}<button className="button-primary" onClick={()=>api<any>("/api/discovery/business-case",{method:"PUT",body:JSON.stringify({values:v})})}>Save baseline</button></section><section className="panel exposure"><span className="eyebrow">CURRENT SYNTHETIC BASELINE EXPOSURE</span><h3>Annual view</h3><div className="exposure-row"><span>Applications / year</span><b>{Math.round(v.applications_per_year)}</b></div><div className="exposure-row"><span>Current manual annual hours</span><b>{Math.round(v.annual_manual_hours)} h</b></div><div className="exposure-row"><span>Estimated annual returned cases</span><b>{Math.round(v.estimated_annual_returned_cases)}</b></div><div className="exposure-row"><span>Estimated rework hours</span><b>{Math.round(v.estimated_rework_hours)} h</b></div><div className="exposure-row highlighted"><span>Indicative labour cost</span><b>QAR {Math.round(v.indicative_labour_cost_qar).toLocaleString()}</b></div><p>These are illustrative calculations, not a savings or ROI claim.</p></section></div></>}
function Privacy(){return <><PageIntro kicker="CONTROL GATE" title="Privacy & data access" description="No real sensitive data is authorized in DEV or TEST. This page makes the current boundary explicit."/><div className="privacy-banner"><div className="shield">✓</div><div><b>REAL SENSITIVE-DOCUMENT PROCESSING: NOT APPROVED</b><p>Development policy is synthetic / redacted only. The external AI route is disabled until approved.</p></div></div><section className="decision-table panel">{[["Third-party processing of owner/QID/title deed data","UNKNOWN"],["Approved data location","UNKNOWN"],["Remote/offshore raw-data access","UNKNOWN"],["External AI route","DISABLED UNTIL APPROVED"],["Approved TEST real-document location","UNKNOWN"],["Development policy","SYNTHETIC / REDACTED ONLY"]].map(([a,b])=><div key={a}><span>{a}</span><b className={b.includes("DISABLED")||b.includes("SYNTHETIC")?"safe":"unknown"}>{b}</b></div>)}</section></>}
function Volume(){const [v,setV]=useState<any>(null);useEffect(()=>{api<any>("/api/discovery/volume").then(setV).catch(()=>{})},[]);if(!v)return <Loading/>;return <><PageIntro kicker="EVIDENCE FOR LATER DESIGN" title="Volume / concurrency baseline" description="Synthetic editable values only. No sophisticated concurrency implementation exists in Week 1."/><div className="volume-grid">{Object.entries(v).map(([k,val])=><label className="volume-card" key={k}><span>{k.replaceAll("_"," ")}</span><input value={String(val)} onChange={e=>setV({...v,[k]:e.target.value})}/><small>{k.includes("concurrent")?"Informs whether a simple DB lease is enough later.":"Replace during Phase 0 validation."}</small></label>)}</div><button className="button-primary" onClick={()=>api("/api/discovery/volume",{method:"PUT",body:JSON.stringify({values:v})})}>Save volume baseline</button></>}
function Inquiries(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any[]>("/api/ministry-inquiries").then(setItems).catch(()=>{})},[]);const ask=(i:any)=>api<any>(`/api/ministry-inquiries/${i.id}`,{method:"PATCH",body:JSON.stringify({status:i.status==='NOT_ASKED'?"ASKED":"NOT_ASKED"})}).then(x=>setItems(items.map(a=>a.id===x.id?x:a)));return <><PageIntro kicker="NARROW QUESTIONS" title="Ministry inquiry log" description="Process-quality questions for the client to validate. This is not a request to automate an official website."/><section className="panel inquiry-list">{items.map(i=><div className="inquiry-row" key={i.id}><div><span className="eyebrow">{i.question_code}</span><b>{i.question}</b><small>Owner: {i.client_owner||"TBD"}</small></div><button className={`decision-status ${i.status.toLowerCase()}`} onClick={()=>ask(i)}>{i.status}</button></div>)}</section></>}
function Raid(){const [items,setItems]=useState<Raid[]>([]);const [title,setTitle]=useState("");useEffect(()=>{api<Raid[]>("/api/raid").then(setItems).catch(()=>{})},[]);const add=()=>{if(!title)return;api<Raid>("/api/raid",{method:"POST",body:JSON.stringify({type:"ISSUE",title,description:title,severity:"MEDIUM",owner:"TBD",status:"OPEN",mitigation:"To be confirmed"})}).then(x=>{setItems([x,...items]);setTitle("")})};return <><PageIntro kicker="DELIVERY CONTROL" title="RAID log" description="Risks, assumptions, issues, and dependencies for the Week 1 foundation."/><div className="raid-add"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Add a synthetic issue or dependency"/><button className="button-primary" onClick={add}>Add item</button></div><section className="panel raid-list">{items.map(i=><div className="raid-row" key={i.id}><span className={`raid-type ${i.type.toLowerCase()}`}>{i.type}</span><div><b>{i.title}</b><p>{i.description}</p><small>Owner: {i.owner} · Mitigation: {i.mitigation}</small></div><span className="tag">{i.status}</span></div>)}</section></>}
function Loading(){return <div className="loading">Loading synthetic baseline…</div>}

export default App;
