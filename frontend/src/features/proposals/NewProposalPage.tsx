import { useRef, useState } from "react";
import { api } from "../../api";
import { Icon } from "../../Icon";
import { proposalHeaders } from "./api";
import { proposalSourceRegistry } from "./registry";
import type { JsonRecord, ProposalRole, ProposalSourceOption } from "./types";

export function NewProposalPage({ role, onBack, onCreated }: { role: ProposalRole; onBack: () => void; onCreated: (id: string) => void }) {
  const [source, setSource] = useState<ProposalSourceOption | null>(null);
  const [form, setForm] = useState({ title: "", client: "", projectReference: "", sourceTitle: "", sourceDate: "", notes: "", email: "", contact: "" });
  const [file, setFile] = useState<File | null>(null);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState("");
  const fileRef = useRef<HTMLInputElement>(null);
  const update = (key: keyof typeof form, value: string) => setForm((current) => ({ ...current, [key]: value }));
  const create = async () => {
    if (!form.title.trim() || !form.client.trim()) { setError("Proposal title and Client are required."); return; }
    if (!source) { setError("Choose how this Proposal began."); return; }
    if (source.acceptsFile && !file) { setError(`Select the ${source.label.toLowerCase()} to register as initial evidence.`); return; }
    setBusy(true); setError("");
    try {
      const idempotency = `p04-${Date.now()}-${form.title.trim().slice(0, 32)}`;
      let result: JsonRecord;
      if (source.acceptsFile && file) {
        const body = new FormData();
        body.append("proposal_description", form.title.trim()); body.append("project_reference", form.projectReference); body.append("client_name", form.client.trim()); body.append("initial_source_type", source.key); body.append("source_title", form.sourceTitle || file.name); body.append("source_date", form.sourceDate); body.append("source_notes", form.notes); body.append("idempotency_key", idempotency); body.append("file", file);
        result = await api<JsonRecord>("/api/bd/proposals/intake", { method: "POST", headers: proposalHeaders(role), body });
      } else {
        result = await api<JsonRecord>("/api/bd/proposals", { method: "POST", headers: { ...proposalHeaders(role), "Content-Type": "application/json" }, body: JSON.stringify({ proposal_description: form.title.trim(), project_reference: form.projectReference || undefined, client_name: form.client.trim(), idempotency_key: idempotency }) });
      }
      const id = typeof result.id === "string" ? result.id : typeof result.proposal === "object" && result.proposal && typeof (result.proposal as JsonRecord).id === "string" ? (result.proposal as JsonRecord).id as string : "";
      if (!id) throw new Error("PROPOSAL_CREATE_RESPONSE_INVALID");
      onCreated(id);
    } catch (cause: unknown) { setError(cause instanceof Error ? cause.message : "Proposal could not be created. No source was claimed."); } finally { setBusy(false); }
  };
  return <div className="proposal-feature-page proposal-new-page"><header className="proposal-page-intro"><div><span className="eyebrow">PROPOSALS · INTAKE</span><h2>New Proposal</h2><p>Start with the source or client context that created the commercial enquiry. The Proposal remains valid if source registration needs a retry.</p></div><button type="button" className="button-secondary" onClick={onBack}><Icon name="arrow-left" size={14} /> Proposal register</button></header>{error && <div className="proposal-alert error-state" role="alert"><Icon name="alert" size={17} /> {error}</div>}<section className="panel source-choice-panel"><div className="panel-head"><div><span className="eyebrow">SOURCE-FIRST INTAKE</span><h3>How did this Proposal begin?</h3></div><small>Source family changes the capture controls and backend classification.</small></div><div className="source-option-grid">{proposalSourceRegistry.map((option) => <button type="button" key={option.key} className={source?.key === option.key ? "source-option selected" : "source-option"} onClick={() => { setSource(option); setFile(null); if (fileRef.current) fileRef.current.value = ""; }}><span className="source-option-icon"><Icon name={option.icon === "mail" ? "briefcase" : option.icon === "document" ? "library" : option.icon === "image" ? "current" : option.icon === "users" ? "users" : "empty"} size={19} /></span><span><b>{option.label}</b><small>{option.description}</small></span>{source?.key === option.key && <Icon name="check" size={17} />}</button>)}</div></section>{source && <section className="panel proposal-form-panel"><div className="panel-head"><div><span className="eyebrow">{source.label.toUpperCase()}</span><h3>New Proposal from {source.label}</h3></div><span className="tag">{source.key}</span></div><div className="proposal-form-grid"><label>Proposal title<input aria-label="Proposal title" value={form.title} onChange={(event) => update("title", event.target.value)} placeholder="Commercial enquiry or tender name" /></label><label>Client<input aria-label="Client" value={form.client} onChange={(event) => update("client", event.target.value)} placeholder="Existing Client or candidate name" /></label><label>Project reference <span className="field-hint">provisional if no Project exists</span><input aria-label="Project reference" value={form.projectReference} onChange={(event) => update("projectReference", event.target.value)} placeholder="Project or client reference" /></label>{source.key === "CLIENT_DATA" && <><label>Proposal contact<input aria-label="Proposal contact" value={form.contact} onChange={(event) => update("contact", event.target.value)} placeholder="Contact name" /></label><label>Contact email<input aria-label="Contact email" value={form.email} onChange={(event) => update("email", event.target.value)} placeholder="name@client.example" type="email" /></label></>}{source.key !== "NONE" && <><label>Source title<input aria-label="Source title" value={form.sourceTitle} onChange={(event) => update("sourceTitle", event.target.value)} placeholder={source.acceptsFile ? "Title or filename" : "Client information source"} /></label><label>Source date<input aria-label="Source date" value={form.sourceDate} onChange={(event) => update("sourceDate", event.target.value)} type="date" /></label><label className="proposal-form-wide">Notes<textarea aria-label="Source notes" value={form.notes} onChange={(event) => update("notes", event.target.value)} placeholder="Human-entered context; not verified external fact" /></label></>}{source.acceptsFile && <label className="proposal-file-drop proposal-form-wide">{source.label}<input ref={fileRef} aria-label={`${source.label} file`} type="file" accept={source.key === "TENDER_PHOTO" ? "image/*" : undefined} onChange={(event) => setFile(event.target.files?.[0] || null)} /><span>{file ? `${file.name} · ${(file.size / 1024).toFixed(0)} KB` : "Choose a file to register as evidence"}</span></label>}</div><div className="proposal-form-footer"><p><Icon name="shield" size={14} /> Document, DocumentVersion, hash, and provenance remain in the existing governed source system. No OCR or AI certainty is implied by upload.</p><button type="button" className="button-primary" onClick={() => void create()} disabled={busy}>{busy ? "Creating Proposal…" : "Create Proposal draft"}</button></div></section>}{!source && <div className="proposal-empty panel"><b>Choose a starting point to continue.</b><span>You can also start without a source and add evidence from the Proposal workspace.</span></div>}</div>;
}
