import { useEffect, useMemo, useState, type FormEvent, type ReactNode } from "react";
import { api } from "./api";
import "./permit-authority-ux.css";

type Catalog = { id: string; code: string; name_en: string; name_ar?: string | null };
type Portfolio = { items: any[]; total: number; page: number; page_size: number; lanes: Record<string, number> };
type Context = { projects: any[]; journeys: any[]; external_bodies: Catalog[]; jurisdictions: Catalog[]; service_types: Catalog[]; scope_note: string };
const emptyPortfolio: Portfolio = { items: [], total: 0, page: 1, page_size: 25, lanes: { all: 0, need_action: 0, authority_review: 0, ready_close: 0 } };
function normalizePortfolio(value: unknown): Portfolio | null {
  if (!value || typeof value !== "object") return null;
  const candidate = value as Partial<Portfolio>;
  if (!Array.isArray(candidate.items) || !candidate.lanes || typeof candidate.lanes !== "object") return null;
  const lanes = candidate.lanes as Record<string, unknown>;
  return {
    items: candidate.items,
    total: typeof candidate.total === "number" ? candidate.total : candidate.items.length,
    page: typeof candidate.page === "number" ? candidate.page : 1,
    page_size: typeof candidate.page_size === "number" ? candidate.page_size : 25,
    lanes: {
      all: typeof lanes.all === "number" ? lanes.all : 0,
      need_action: typeof lanes.need_action === "number" ? lanes.need_action : 0,
      authority_review: typeof lanes.authority_review === "number" ? lanes.authority_review : 0,
      ready_close: typeof lanes.ready_close === "number" ? lanes.ready_close : 0,
    },
  };
}
const pretty = (value: unknown) => String(value ?? "—").replaceAll("_", " ");
const label = (item?: Catalog | null) => item?.name_en || item?.code || "Pending";
const move = (path: string) => { window.history.pushState({}, "", path); window.dispatchEvent(new PopStateEvent("popstate")); };
function Status({ value }: { value: string }) { return <span className={`status status-${value.toLowerCase().replaceAll("_", "-")}`}>{pretty(value)}</span>; }

export function PermitPortfolioPage() {
  const [data, setData] = useState<Portfolio | null>(null); const [lane, setLane] = useState(""); const [q, setQ] = useState(""); const [error, setError] = useState("");
  const load = () => api<unknown>(`/api/permit-ux/portfolio?lane=${encodeURIComponent(lane)}&q=${encodeURIComponent(q)}`).then((value) => {
    const normalized = normalizePortfolio(value);
    if (!normalized) {
      setData(emptyPortfolio);
      setError("Permit portfolio response was unavailable; showing an empty case view.");
      return;
    }
    setData(normalized);
    setError("");
  }).catch((e) => { setData(emptyPortfolio); setError(e instanceof Error ? e.message : "Permit portfolio unavailable."); });
  useEffect(() => { void load(); }, [lane]);
  return <div className="permit-ux-page"><div className="page-intro permit-ux-intro"><div><span className="eyebrow">OWNER OPERATIONS · AUTHORITY CASE PORTFOLIO</span><h2>Permits</h2><p>Canonical Authority Cases with derived stage, status, blockers, comments, and identifiers.</p></div><div className="permit-ux-actions"><button className="button-secondary" onClick={() => window.open("/api/permit-ux/exports/permit-tracker.csv", "_blank")}>Export tracker</button><button className="button-primary" onClick={() => move("/permits/new")}>New Permit</button></div></div><div className="synthetic-note">PORTAL AUTOMATION DEFERRED BY SCOPE · HUMAN SUBMISSION REQUIRED · END DATE NOT CONFIGURED UNLESS CANONICAL SOURCE EXISTS</div>{error && <div className="error-banner">{error}</div>}<div className="permit-lanes" role="tablist" aria-label="Permit work lanes">{[["", "All Permits", data?.lanes.all], ["NEED_ACTION", "Need Action", data?.lanes.need_action], ["AUTHORITY_REVIEW", "Authority Review", data?.lanes.authority_review], ["READY_CLOSE", "Ready / Close", data?.lanes.ready_close]].map(([value, name, count]) => <button key={String(name)} className={lane === value ? "permit-lane active" : "permit-lane"} onClick={() => setLane(String(value))} role="tab" aria-selected={lane === value}><b>{String(count ?? "—")}</b><span>{name}</span></button>)}</div><section className="panel permit-portfolio-panel"><form className="permit-toolbar" onSubmit={(e) => { e.preventDefault(); void load(); }}><label className="permit-search">Search project, permit, or case<input value={q} onChange={(e) => setQ(e.target.value)} placeholder="e.g. project reference" /></label><button className="button-secondary" type="submit">Search</button></form>{data === null ? <p className="permit-empty">Loading permit portfolio…</p> : data.items.length === 0 ? <div className="permit-empty"><h3>No permits in this view</h3><p>Start an explicit Authority Case from an activated Project, or adjust the lane/search.</p><button className="button-primary" onClick={() => move("/permits/new")}>Start a Permit Case</button></div> : <div className="permit-table-wrap"><table className="permit-table"><thead><tr><th>Project</th><th>Journey</th><th>Permit / Case Ref</th><th>Stage</th><th>End Date</th><th>System Status</th><th>Block</th><th>Open Comments</th><th /></tr></thead><tbody>{data.items.map((item) => <tr key={item.case_id}><td><b>{item.project_name}</b><small>{item.project_reference}</small></td><td>{item.journey_code || "—"}</td><td><b>{item.permit_identifier?.value || "Pending"}</b><small>{item.case_reference}</small></td><td>{pretty(item.stage)}</td><td title="Canonical target date not configured">{item.end_date || "—"}</td><td><Status value={item.system_status} /></td><td>{item.block_count ? <span className="block-count">{item.block_count} blocker{item.block_count === 1 ? "" : "s"}</span> : "—"}</td><td>{item.open_comments ? <span className="comment-count">{item.open_comments}</span> : "—"}</td><td><button className="link-button" onClick={() => move(`/permits/${item.case_id}/overview`)}>Open</button></td></tr>)}</tbody></table></div>}</section></div>;
}

export function NewPermitPage() {
  const [context, setContext] = useState<Context | null>(null); const [projectId, setProjectId] = useState(""); const [journeyId, setJourneyId] = useState(""); const [bodyId, setBodyId] = useState(""); const [jurisdictionId, setJurisdictionId] = useState(""); const [serviceId, setServiceId] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  useEffect(() => { api<Context>("/api/permit-ux/new/context").then((value) => { setContext(value); setProjectId(value.projects[0]?.id || ""); setJourneyId(value.journeys[0]?.id || ""); setBodyId(value.external_bodies[0]?.id || ""); setJurisdictionId(value.jurisdictions[0]?.id || ""); setServiceId(value.service_types[0]?.id || ""); }).catch((e) => setError(e instanceof Error ? e.message : "New Permit context unavailable.")); }, []);
  const project = context?.projects.find((item) => item.id === projectId);
  const start = async (e: FormEvent) => { e.preventDefault(); setBusy(true); setError(""); try { const result = await api<any>("/api/authority-cases", { method: "POST", body: JSON.stringify({ project_id: projectId, regulatory_journey_id: journeyId || undefined, external_body_id: bodyId, jurisdiction_id: jurisdictionId, service_type_id: serviceId, subject_type: "Project", subject_id: projectId, idempotency_key: `permit-ux-start-${projectId}-${journeyId || "new"}-${bodyId}-${jurisdictionId}-${serviceId}` }) }); move(`/permits/${result.case.id}/overview`); } catch (cause) { setError(cause instanceof Error ? cause.message : "Case creation was blocked."); } finally { setBusy(false); } };
  return <div className="permit-ux-page"><div className="page-intro"><div><span className="eyebrow">SKETCH 11 · GUIDED START</span><h2>New Permit</h2><p>Choose an activated Project, select or create its Regulatory Journey, review context, then explicitly start an Authority Case.</p></div><button className="button-secondary" onClick={() => move("/permits")}>Back to permits</button></div>{error && <div className="error-banner">{error}</div>}<form className="panel new-permit-form" onSubmit={start}><label>Activated Project<select value={projectId} onChange={(e) => setProjectId(e.target.value)} required><option value="">Select a project</option>{context?.projects.map((item) => <option key={item.id} value={item.id}>{item.project_reference} · {item.project_name}</option>)}</select></label>{project && <div className="context-card"><span className="eyebrow">PROJECT CONTEXT</span><h3>{project.project_name}</h3><p>{project.project_reference} · {project.municipality} · {project.permit_type}</p><small>No free-text project is accepted. Proposal scope remains advisory until explicitly confirmed.</small></div>}<label>Regulatory Journey<select value={journeyId} onChange={(e) => setJourneyId(e.target.value)}><option value="">Create new journey for this case</option>{context?.journeys.map((item) => <option key={item.id} value={item.id}>{item.journey_code} · {item.status}</option>)}</select></label><div className="new-permit-grid"><label>External body<select value={bodyId} onChange={(e) => setBodyId(e.target.value)} required>{context?.external_bodies.map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select></label><label>Jurisdiction<select value={jurisdictionId} onChange={(e) => setJurisdictionId(e.target.value)} required>{context?.jurisdictions.map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select></label><label>Service type<select value={serviceId} onChange={(e) => setServiceId(e.target.value)} required>{context?.service_types.map((item) => <option key={item.id} value={item.id}>{label(item)}</option>)}</select></label></div><div className="scope-note">{context?.scope_note || "Accepted proposal scope is advisory; explicit authority selection is required."}</div><button className="button-primary" disabled={busy || !projectId}>{busy ? "Starting…" : "Start Authority Case"}</button></form></div>;
}

const tabs = ["overview", "project-details", "requirements", "documents", "drawings", "forms", "comments", "submission-history", "permit-license", "history"];
export function PermitCasePage() {
  const parts = window.location.pathname.split("/"); const caseId = parts[2] || ""; const [tab, setTab] = useState(parts[3] || "overview"); const [workspace, setWorkspace] = useState<any | null>(null); const [error, setError] = useState(""); const [assistForms, setAssistForms] = useState<any[] | null>(null); const [assistPreview, setAssistPreview] = useState<any | null>(null); const [assistError, setAssistError] = useState(""); const [assistBusy, setAssistBusy] = useState(false);
  useEffect(() => { if (caseId) api<any>(`/api/permit-ux/cases/${caseId}`).then(setWorkspace).catch((e) => setError(e instanceof Error ? e.message : "Permit workspace unavailable.")); }, [caseId]);
  const loadAssistForms = async () => { setAssistBusy(true); setAssistError(""); try { setAssistForms(await api<any[]>("/api/master-content?content_type=FORM&module=PERMIT&automation_readiness=AUTOMATED_USE_READY")); } catch (cause) { setAssistError(cause instanceof Error ? cause.message : "Eligible forms unavailable."); } finally { setAssistBusy(false); } };
  const previewAssist = async (form: any) => { setAssistBusy(true); setAssistError(""); try { setAssistPreview(await api<any>("/api/governed-prefill/preview", { method: "POST", body: JSON.stringify({ master_content_id: form.id, context_entity_type: "AuthorityCase", context_entity_id: caseId, purpose: "FORM_PREPARATION" }) })); } catch (cause) { setAssistPreview(null); setAssistError(cause instanceof Error ? cause.message : "Assist preview unavailable."); } finally { setAssistBusy(false); } };
  const content = useMemo(() => { if (!workspace) return null; if (tab === "overview") return <><section className="workspace-summary"><div><span className="eyebrow">{workspace.case.case_reference}</span><h2>{workspace.project.project_name}</h2><p>{workspace.journey?.journey_code || "Journey pending"} · {label(workspace.external_body)} · {label(workspace.service_type)} · {label(workspace.jurisdiction)}</p></div><Status value={workspace.status.system_status} /></section><div className="workspace-metrics">{[["Stage", pretty(workspace.status.stage)], ["Blockers", workspace.status.block_count], ["Open Comments", workspace.status.open_comments], ["Permit Number", workspace.permit_identifier?.value || "Pending"]].map(([name, value]) => <div className="metric" key={String(name)}><span>{name}</span><strong>{String(value)}</strong><small>Canonical projection</small></div>)}</div><section className="panel next-action"><span className="eyebrow">NEXT ACTION</span><h3>{workspace.next_action}</h3><p>System status is derived from requirements, evidence, baseline, precheck, findings, and submission state.</p></section></>;
    if (tab === "project-details") return <Detail title="Project Details"><Info rows={[["Project reference", workspace.project.project_number], ["Project", workspace.project.project_name], ["Municipality", workspace.project.municipality], ["Permit type", workspace.project.permit_type], ["Property", workspace.project_details.properties.map((x: any) => `${x.plot_number} · ${x.municipality}`).join(", ") || "Not configured"], ["Party / client", workspace.project_details.parties.map((x: any) => x.name_en || x.name_ar).join(", ") || "Not configured"]]} /><p className="muted">{workspace.project_details.sensitive_fields}</p><section className="context-card"><span className="eyebrow">PARTIES &amp; REPRESENTATION</span>{(workspace.parties_representation?.assignments || []).length ? workspace.parties_representation.assignments.map((x: any) => <p key={x.id}><b>{pretty(x.role_code)}</b> · {x.party?.name_en || x.party?.name_ar || "Party"} · {pretty(x.status)}</p>) : <p>No case-scoped party roles confirmed.</p>}<small>Commercial Client, Owner, Applicant, Agent, Consultant, Contractor, Signatory, and Regulatory Contact remain distinct unless explicitly assigned.</small></section><section className="context-card"><span className="eyebrow">REGULATORY CONTACT</span>{(workspace.parties_representation?.contacts || []).filter((x: any) => x.purpose === "REGULATORY").length ? <p>Verified purpose-specific contact is recorded.</p> : <p>No verified regulatory-purpose contact; general mobile is not substituted.</p>}</section></Detail>;
    if (tab === "requirements") return <Detail title="Requirements"><Rows items={workspace.requirements} render={(x: any) => <><b><Status value={x.status} /></b><small>{pretty(x.applicability)} · {x.reason}</small></>} /></Detail>;
    if (tab === "documents") return <Detail title="Documents"><Rows items={workspace.documents} render={(x: any) => <><b>{x.document.logical_name}</b><small>{x.versions.length} immutable version(s) · canonical View / Upload / Replace controls</small></>} /></Detail>;
    if (tab === "drawings") return <Detail title="Drawings"><Rows items={workspace.drawings} render={(x: any) => <><b>{x.deliverable?.deliverable_ref || "Drawing"} · {x.revision?.title || "Untitled"}</b><small>{x.revision?.revision_code || "Revision pending"} · {x.discipline || "Discipline not configured"} · professional approval {x.professional_approval?.status || "not evidenced"}</small></>} /></Detail>;
    if (tab === "forms") return <Detail title="Forms"><div className="assist-toolbar"><div><span className="eyebrow">EXPLICIT CASE ACTION</span><p>Review governed suggestions for this Authority Case. No form or canonical content is changed.</p></div><button className="button-primary" onClick={() => { setAssistPreview(null); void loadAssistForms(); }} disabled={assistBusy}>{assistBusy ? "Loading…" : "Assist with Form"}</button></div><Rows items={workspace.forms} render={(x: any) => <><b>{x.form.context_type} form</b><small>{pretty(x.form.status)} · generated artifacts {x.generated_artifacts.length} · signatures/stamps remain human gates</small></>} />{assistError && <div className="error-banner">{assistError}</div>}{assistForms && <div className="assist-panel"><h4>Eligible canonical forms</h4>{assistForms.length ? assistForms.map((form: any) => <div className="assist-choice" key={form.id}><div><b>{form.ref} · {form.title}</b><small>{form.owner_status} · current version {form.current_version_id || form.document_version_id || "not pinned"}</small></div><button className="button-secondary" onClick={() => void previewAssist(form)} disabled={assistBusy}>Review suggestion</button></div>) : <p>No eligible governed form is available for this case.</p>}{assistPreview && <div className="assist-preview"><div className="assist-preview-head"><div><span className="eyebrow">GOVERNED PREFILL PREVIEW</span><h4>{assistPreview.master_content_ref}</h4></div><Status value={assistPreview.preview_status} /></div><p>Master version {assistPreview.document_version_id} · mapping release {assistPreview.mapping_release_id} · currentness {assistPreview.staleness_state}</p>{assistPreview.fields.map((field: any) => <div className="assist-field" key={field.mapping_rule_id}><div><b>{field.target_field}</b><small>{field.proposal_status} · {field.warning || "Citation-backed value"}</small></div><strong>{field.proposed_value == null ? "Not proposed" : String(field.proposed_value)}</strong><details><summary>Evidence &amp; citation</summary>{field.citations.map((citation: any) => <small key={citation.canonical_entity_id}>{citation.canonical_entity_type} {citation.canonical_entity_id} · {citation.locator} · version {citation.document_version_id}</small>)}</details></div>)}<div className="scope-note">Preview only. Apply to draft is deferred pending a reviewed safe command. Signatures, stamps, approvals, and authority actions remain human-controlled.</div></div>}</div>}</Detail>;
    if (tab === "comments") return <Detail title="Comments / Findings"><Rows items={workspace.comments} render={(x: any) => <><b>{x.finding.title}</b><small>{pretty(x.finding.status)} · {pretty(x.finding.severity)} · {x.finding.raw_text}</small><small>{x.responses.length} internal response(s); AI boundary: {x.ai_boundary}</small></>} /></Detail>;
    if (tab === "submission-history") return <Detail title="Submission History"><Rows items={workspace.submission_history.cycles} render={(x: any) => <><b>C{x.cycle_number}</b><small>{pretty(x.status)} · immutable package/snapshot boundary</small></>} /></Detail>;
    if (tab === "permit-license") return <Detail title="Permit / License"><div className="context-card"><h3>{workspace.permit_identifier?.value || "No authority identifier"}</h3><p>{workspace.permit_license.outcomes.length ? "Authority outcome is evidence-backed." : "No evidence-backed authority outcome has been recorded."}</p><small>System summary only. This surface does not create an official permit or construction authorization.</small></div></Detail>;
    return <Detail title="History"><Rows items={workspace.history} render={(x: any) => <><b>{x.event_type}</b><small>{x.created_at} · {x.actor_id}</small></>} /></Detail>;
  }, [assistBusy, assistError, assistForms, assistPreview, tab, workspace]);
  return <div className="permit-ux-page">{error && <div className="error-banner">{error}</div>}{!workspace ? <p className="permit-empty">Loading permit workspace…</p> : <><div className="page-intro"><div><span className="eyebrow">PERMIT / AUTHORITY CASE WORKSPACE</span><h2>{workspace.case.case_reference}</h2><p>{workspace.project.project_name}</p></div><button className="button-secondary" onClick={() => move("/permits")}>Back to permits</button></div><nav className="workspace-tabs" aria-label="Permit workspace tabs">{tabs.map((item) => <button className={tab === item ? "active" : ""} key={item} onClick={() => { setTab(item); move(`/permits/${caseId}/${item}`); }}>{pretty(item)}</button>)}</nav>{content}</>}</div>;
}
function Detail({ title, children }: { title: string; children: ReactNode }) { return <section className="panel permit-detail"><div className="panel-head"><div><span className="eyebrow">CANONICAL READ MODEL</span><h3>{title}</h3></div></div>{children}</section>; }
function Info({ rows }: { rows: [string, any][] }) { return <div className="info-grid">{rows.map(([name, value]) => <div key={name}><span>{name}</span><b>{String(value || "—")}</b></div>)}</div>; }
function Rows({ items, render }: { items: any[]; render: (item: any) => ReactNode }) { return items.length ? <div className="ux-card-list">{items.map((item, index) => <div className="ux-card" key={item.id || index}>{render(item)}</div>)}</div> : <p className="permit-empty">Nothing has been recorded yet.</p>; }
